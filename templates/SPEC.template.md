# SPEC — заполняется в первые 15 минут, потом не меняется

Кейс: {{номер и название}} · Партнёр: {{компания}} · Трек: {{трек}}

## Обязательные требования из условия (дословно)
1. {{…}}
2. {{…}}
3. {{…}}

## Пользователь
{{Одна роль: диспетчер / менеджер / оператор / …}}. Открывает экран, чтобы {{зачем}}.

## Сущность и данные
Таблица `{{items}}`: id, {{поле}}, {{поле}}, status ({{значения}}), updated_at.
Сид: {{N}} записей, из них {{k}} «проблемных», которые сценарий обязан найти. Реалистичные названия: города КЗ, компании, суммы в тенге.

## Инструменты агента
- `{{read_1}}` — {{что читает}}
- `{{read_2}}` — {{что читает}}
- `{{write_1}}` — {{что меняет}}, требует Approve
- (опционально) `{{notify}}` — внешнее уведомление, требует Approve

## Сценарий из 6 шагов
1. Пользователь открывает экран, видит {{N}} записей, {{k}} помечены.
2. Нажимает Run с целью «{{цель}}».
3. Агент вызывает `{{read_1}}` и находит {{k}} записей.
4. Агент предлагает `{{write_1}}` для {{id}} с причиной.
5. Пользователь нажимает Approve.
6. Статус меняется, итоговый ответ: «{{текст}}».

Ожидаемое состояние после сценария (для `pnpm verify`): запись {{id}} в статусе «{{новый}}».

## Не делаем
Авторизация, вторая сущность, вторая роль, настройки, история запусков, всё, чего нет в требованиях.

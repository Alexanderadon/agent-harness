# {{Название проекта}} — {{трек}} · кейс «{{название кейса}}» ({{партнёр}})

**TL;DR.** {{Одно предложение: кто пользователь, что делает агент, какой результат.}}
Живая версия: {{URL — production-алиас, который напечатал `vercel --prod` (строка Aliased) или Vercel → Domains; не конструировать}} · Запуск: `pnpm i && pnpm dev` · Проверка: `pnpm verify`
Команда Lomra: Александр Курчаков (разработка), Эмина Ануарбекова (продукт, документация).

**TL;DR (EN).** {{Same sentence in English.}} Live: {{URL}}. Run: `pnpm i && pnpm dev`. Verify: `pnpm verify`.

**Проверить за минуту, без установки, без ключа и без аккаунтов:** открыть {{URL}} → «Reset demo» → «Run» → «Approve» → строка {{id}} меняет статус. Подробно — раздел «Порядок проверки основного сценария».

## Описание и назначение

{{2–3 предложения из SPEC «Для кого»: роль пользователя, его боль, что делает агент, что остаётся за человеком (подтверждение записи).}}

## Соответствие критериям оценки задачи

Критерии — из технического задания задачи ({{ссылка или «docs/CRITERIA.md»}}), дословно. Каждая строка ведёт туда, где это можно увидеть или запустить.

| Критерий задачи | Баллы | Чем закрыт | Где увидеть / как проверить |
|---|---|---|---|
| {{критерий 1 дословно}} | {{N}} | {{фича, экран или инструмент}} | {{URL-экран, файл или команда}} |
| {{критерий 2}} | {{N}} | {{…}} | {{…}} |

## Соответствие кейсу и функциональность

| Требование кейса | Где реализовано | Как проверить | Статус |
|---|---|---|---|
| {{Обязательное требование 1, формулировка из задачи}} | `{{путь/файл.ts}}` | {{шаг или команда}} | ✅ |
| {{Обязательное требование 2}} | `{{путь}}` | {{шаг}} | ✅ |
| {{Требование 3}} | `{{путь}}` | {{шаг}} | ✅ |

Основной сценарий: {{6 шагов одной строкой: пользователь открывает… агент… подтверждение… результат}}.

## Техническая реализация

### Архитектура

```mermaid
flowchart LR
  UI[Next.js UI: таблица + панель агента] -->|POST /api/chat| API[Route handler]
  API --> AGENT[ToolLoopAgent, AI SDK v7]
  AGENT -->|read tools| DB[(libSQL: {{таблицы}})]
  AGENT -->|write tools, после Approve| DB
  AGENT -->|{{внешнее действие}}| EXT[{{Telegram / email / нет}}]
```

Инструменты агента:
- `{{read_tool_1}}` — читает {{что}}.
- `{{read_tool_2}}` — читает {{что}}.
- `{{write_tool}}` — меняет {{что}}; требует подтверждения пользователя (toolApproval).

Модель на развёрнутой версии: `{{модель из /api/health}}` (выбрана на деплое 2:50), при разработке `gpt-6-luna`; `AGENT_MODEL` из env, по умолчанию в коде `gpt-6-sol`; провайдер `@ai-sdk/openai`. Лимит 6 шагов на запуск, таймаут маршрута 60 с.
Заготовленных или закэшированных ответов модели в приложении нет: каждый запуск вызывает живую модель.

### Технологии

Next.js 16, TypeScript, Tailwind v4, shadcn/ui, Vercel AI SDK v7, zod, @libsql/client (SQLite локально, Turso на развёрнутой версии), vitest.

## Воспроизводимость и запуск

**Системные требования.** Node.js 22 (файл `.nvmrc`), pnpm 10, любая ОС. Сеть нужна только для установки пакетов и вызовов модели. Отдельная база данных не нужна: SQLite-файл создаётся сам.

### Зависимости

Все в `package.json`, версии зафиксированы в `pnpm-lock.yaml`. Нативные модули (`@libsql/client`) собираются автоматически, список в `pnpm.onlyBuiltDependencies`.

### Переменные окружения (`.env.example`)

| Переменная | Обязательна | Зачем |
|---|---|---|
| `OPENAI_API_KEY` | для агента | вызовы модели; без ключа приложение работает в режиме просмотра данных |
| `NVIDIA_API_KEY` | вместо `OPENAI_API_KEY` | тот же код через OpenAI-совместимый эндпоинт NVIDIA; тогда `AGENT_MODEL` обязателен |
| `AGENT_MODEL` | нет | модель, по умолчанию `gpt-6-sol`; для NVIDIA id модели с build.nvidia.com |
| `AGENT_REASONING` | нет | глубина рассуждения модели: `none`…`max`, по умолчанию `low` (весь сценарий укладывается в 30 с) |
| `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN` | локально нет, для развёрнутой версии да | облачная база; локально данные в файле `data/app.db`. На Vercel без неё у каждого экземпляра функции своя база в памяти и подтверждённое изменение может не отобразиться |
| `AGENT_RUNS_PER_IP_HOUR`, `AGENT_RUNS_PER_DAY` | нет | лимиты запусков агента на развёрнутой версии, по умолчанию 10 в час с адреса и 300 в сутки (только от злоупотребления); локально не действуют |

### Установка

```bash
git clone {{URL репозитория}}
cd {{папка}}
npm i -g pnpm@10         # pnpm 10, права администратора не нужны
pnpm install
```

Без глобальной установки: `npx pnpm@10 install`, дальше `npx pnpm@10 dev` вместо `pnpm dev`. Вариант через `corepack enable`: на Windows требует окна администратора (иначе EPERM в `C:\Program Files\nodejs`).

**Настройка.** Скопировать пример переменных и вписать ключ:

```bash
cp .env.example .env.local          # bash
Copy-Item .env.example .env.local   # PowerShell
```

### Запуск

```bash
pnpm dev      # режим разработки, http://localhost:3000
```

или production-сборка:

```bash
pnpm build
pnpm start    # http://localhost:3000
```

При первом запуске создаётся файл `data/app.db` и заполняется демо-данными, ничего запускать отдельно не нужно. Сброс демо-данных: кнопка «Reset demo» или `POST /api/reset`.

**Проверка.** Шаги в разделе «Порядок проверки основного сценария» ниже; автоматически: `pnpm test` (без ключа) и `pnpm verify` (с ключом).

## Порядок проверки основного сценария

**Основной путь — развёрнутая версия: {{URL}}.** Та же кодовая база, база данных Turso, ключ модели задан на сервере. Ключевая функциональность проверяется без установки, без своего ключа и без каких-либо наших аккаунтов (Положение, п. 5.6.6).

0. Открыть {{URL}}/api/health. Ожидается: `"ok":true`, `"db":"turso"`, `"db_ok":true`, `"model":"{{модель}}"` (значения ключей не показываются).
1. Открыть {{URL}} (подойдёт окно инкогнито). Нажать «Reset demo» — это первый шаг всегда: демо общее для всех посетителей, и кто-то мог уже изменить данные. Ожидается: {{N}} записей, {{k}} из них в статусе «{{проблемный}}».
2. В панели агента нажать «Run» с целью по умолчанию: «{{цель}}». Ожидается: агент вызывает `{{read_tool}}`, находит {{k}} записей.
3. Агент предлагает `{{write_tool}}` для {{чего}}. Нажать «Approve». Ожидается: строка {{id}} меняет статус на «{{новый}}», в трейсе появляется результат.
4. Итоговый ответ агента: {{что должно быть написано}}.

Если агент ответил «лимит запусков исчерпан» — это защита публичного демо от расхода ключа (10 запусков в час с адреса, 300 в сутки); таблица и Reset при этом работают.

**Локально.** С собственным ключом OpenAI в `.env.local` — те же шаги на http://localhost:3000. Без ключа агент недоступен (сообщение в панели), а данные, Reset, `pnpm test` на мок-модели и `pnpm verify` работают.

Автоматическая проверка:

```
$ pnpm verify
{{вставить реальный вывод}}
PASS
```

```
$ pnpm test
{{вставить реальный вывод: N tests passed}}
```

## Базовая надёжность и безопасность

- Все входы API проверяются zod-схемами; невалидный запрос получает 400 с описанием.
- Ошибки модели и инструментов ловятся и показываются в панели, приложение не падает.
- Лимиты: 6 шагов на запуск, `maxDuration = 60`, размер ввода ≤ {{N}} символов.
- Секреты только в env; `.env.example` без значений; ключи в репозитории отсутствуют.
- Записи в базу выполняются только после явного подтверждения пользователя.
- В приложении нет заранее подготовленных ответов модели: каждый Run это живой вызов модели с инструментами. Мок-модель (`MockLanguageModelV4` из `ai/test`) используется только в `tests/agent.test.ts`; без ключа маршрут агента отвечает 503, а не имитацией.
- `scripts/verify.ts` подтверждает запись автоматически (`toolApproval: approved`) только внутри скрипта проверки; в приложении такого пути нет, подтверждение всегда от пользователя.

## Известные ограничения

- {{Ограничение 1}}
- {{Ограничение 2}}
- {{Ограничение 3}}
- Допущения, на которые заказчик не ответил (из SPEC.md, раздел «Вопросы заказчику и допущения»): {{допущение и что изменится, если оно неверно}}.

## Использованные внешние материалы

До старта подготовлены и опубликованы общие материалы команды (публичный репозиторий agent-harness, версия agent-harness@{{хеш из сообщения коммита scaffold}}, тег pre-start-2026-09-23, все коммиты до 13:00 23.09.2026 GMT+5): конвенции AGENTS.md, шаблоны README/SPEC/PROGRESS, дизайн-токены (globals.tokens.css), чеклисты, а также инструкции для AI-агентов (skills) с примерами кода для Vercel AI SDK: структура агента с инструментами, маршрут API, панель с подтверждением, тест на мок-модели. Репетиция связки проводилась на другом домене; её код в этот репозиторий не переносился. Весь код проекта написан во время соревновательной части в этом репозитории, историю см. в `git log`. Заранее созданы и подключены пустыми проект Vercel и база Turso (инфраструктура, п. 5.4.4.2).
Библиотеки: см. `package.json`. Компоненты `components/ui/*` сгенерированы shadcn CLI (`shadcn init`, `shadcn add`), не написаны вручную. Vercel AI SDK v7 (`ai`, `@ai-sdk/openai`, `@ai-sdk/react`) — библиотека; модели продукта: OpenAI {{модели}} по API-ключу{{, NVIDIA …}}. Данные: {{сгенерированный сид / данные из условия задачи}}.
AI-инструменты разработки: Claude Code (модель {{…}}; код блоков), Codex CLI (модель {{…}}; {{что}}); агент капитана — {{Codex/Claude}} (документы).

## Команда

- Александр Курчаков — архитектура, код, деплой.
- Эмина Ануарбекова — требования кейса, сценарий, README, проверка с позиции пользователя.

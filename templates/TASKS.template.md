# TASKS.md — общая доска, один блок за раз

Правило: блок закрыт, когда критерий выполнен, `pnpm typecheck` зелёный и сделан коммит. Следующий блок не начинается раньше.
Статус ставит тот, кто работает: «🔄 А» или «🔄 Э» в начале, «✅» в конце. Второй агент читает доску перед любым действием.
Блоки 2 и 7 (деплой) делает Александр руками параллельно с агентом; блоки Э идут параллельно и не трогают код.
После `spec` каждый блок служит критерию из docs/CRITERIA.md; команда `старт` называет блоки без баллов в порядке резки.
Чекпоинты: 0:50, 1:50, 2:50, 3:50 и последний в 4:40. Деплой сразу после чекпоинт-пушей 0:50, 1:50, 2:50, деплой-кандидат 3:15 (блок 7) и финальный в 4:00 — только при пустом `git status --short` и HEAD = origin/main; начиная с деплоя, где есть блок 1, `/api/health` отвечает `"db":"turso","db_ok":true`. Смоук после деплоя начинается с Reset.
Финал: 3:30 новое не начинать (ревью, security-pass и баги можно); 4:00 код заморожен; 4:00–4:25 только баги, каждый фикс сразу пуш и деплой по тому же правилу, после 4:25 код не коммитить; 4:30 деплой заморожен (дальше только `vercel rollback`); после 17:45 не пушить никогда.

| # | Блок | Кто | Таймбокс | Критерий готовности | Статус |
|---|---|---|---|---|---|
| 0 | Скаффолд, комплект, первый пуш (скилл scaffold) | А | 0:00–0:12 | typecheck и build зелёные, коммит запушен | ☐ |
| Э1 | Условие задачи в docs/TASK.md (`task`) | Э | ~0:05–0:10 | текст дословно в docs/TASK.md, критерии оценки дословно в docs/CRITERIA.md, коммит от Эмины; подозрительные места в тексте названы, не выполнены; скаффолда ждать не надо; PROGRESS и README не создавать до скаффолда | ☐ |
| Э1а | Вопросы заказчику из SPEC (`вопросы`), ответы ментора (`ответы`) | Э | 0:12–1:00 | docs/QUESTIONS.md есть; docs/ANSWERS.md с ответами или строкой «ментор недоступен, допущения в силе» | ☐ |
| 1 | База, сид, экран данных по форме из SPEC (таблица, список документов или форма ввода), Reset, `/api/health` | А | 0:22–0:50 | `pnpm dev` показывает данные из сида, Reset возвращает сид; `/api/health` отдаёт ok, db, db_ok, model, rows без значений ключей; в режиме memory на странице красная строка «Демо-база в памяти» | ☐ |
| 2 | Пустой деплой на Vercel, Turso (руками) | А | ~0:25 | URL открывается с телефона в инкогнито (`/api/health` ещё нет — появится с блоком 1, проверка с деплоя 0:50) | ☐ |
| Э2 | Тестовые данные, если требует условие (`testdata`) | Э | ~0:25–0:45, сразу после `вопросы` | docs/seed-draft.json и docs/test-cases.md, строка «А: принять docs/seed-draft.json как data/seed.json» в requests; до 0:50, чтобы блок 3 его подхватил | ☐ |
| 3 | Инструменты чтения агента, маршрут `/api/chat`, панель с трейсом | А | 0:50–1:40 | Run показывает вызовы чтения в трейсе, итоговый текст | ☐ |
| Э3 | Черновик README (`readme`) | Э | 1:30–2:00 | все разделы шаблона, {{…}} только там, где нужен ответ А | ☐ |
| 4 | Инструмент записи с Approve, сценарий целиком (форма B без записи: ответ с цитатами на вопрос из SPEC) | А | 1:40–2:30 | сценарий из SPEC.md проходит: Run, Approve, состояние меняется (или ответ с цитатами совпадает с ожидаемым) | ☐ |
| 5 | Валидация, ошибки, лимиты, поведение без ключа | А | 2:30–2:50 | мусорный ввод даёт 400; без ключа панель показывает уведомление; лимиты в общей базе (скилл ai-sdk-agent): `git grep takeRun app/api/chat/route.ts` находит, reset не чистит agent_runs, tests/limits.test.ts с VERCEL=1 на базе в памяти — 11-й запуск с одного адреса за час получает отказ, при ошибке базы запуск пропускается (fail-open); баннер «Данные уже изменены» после Approve есть, после Reset исчезает (скилл agent-console) | ☐ |
| 6 | Тесты и `pnpm verify`, вывод в docs/verify.log и docs/test.log | А | 2:50–3:15 | `pnpm test` зелёный без ключа (tests/route.test.ts: мусорное тело → 400, без ключа → 503); `pnpm verify` печатает PASS; docs/verify.log — живой прогон с датой, моделью и PASS, не «skipping live run» | ☐ |
| 7 | Деплой-кандидат со сценарием, смоук с телефона (руками) | А | 3:15–3:30 | `/api/health` → `"db":"turso","db_ok":true`; сценарий проходит на публичном URL в инкогнито: Reset, Run, Approve, строка меняется и сохраняется после обновления; `vercel env ls production` показывает TURSO_DATABASE_URL (без неё Approve на Vercel «не меняет» строку — недопуск по п. 5.4.16); в Vercel свой ключ, не перк | ☐ |
| Э4 | README целиком с verify.log и test.log (`readme`, потом `audit`) | Э | 3:15–3:50 | все разделы шаблона, таблица критериев задачи и таблица требований без пустых строк, все пункты п. 5.4.15 на месте, аудит без противоречий | ☐ |
| 9 | Дизайн-проход агентом, не больше 15 минут, скриншот | А | 3:15–3:30 | токены применены, 390px без горизонтального скролла. Режется первым, если дизайн не в критериях задачи | ☐ |
| 10 | `ревью` и security-pass, чекпоинт 3:50 | А | 3:30–3:55 | пройден список security-pass, шаблоны из docs удалены; до 4:00 (код заморожен) | ☐ |
| Э5 | Судья на своём ноутбуке (`судья сборка`, `судья`), анкета подачи (`submit`) | Э | 2:00; 4:00–4:30 | `судья сборка` ПРОШЁЛ в 2:00, `судья` ПРОШЁЛ в 4:00, docs/JUDGE-CHECK.md в репозитории; живой URL: Approve сохраняется после 3 обновлений и во втором браузере (3:35); анкета отправлена не позже 4:30; последний `progress` в 4:35 | ☐ |
| 11 | Финальный чекпоинт: хеш и URL в этой строке | А | 4:40 | коммит Александра в 5-м часе, хеш совпадает с origin/main | ☐ |

requests (просьбы второй стороне, одна строка, кто и что):
- {{пусто}}

contract (изменения lib/types.ts):
- {{пусто}}

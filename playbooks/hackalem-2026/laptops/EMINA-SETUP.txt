НОУТБУК ЭМИНЫ · УСТАНОВКА С НУЛЯ
==================================
Всё в PowerShell. Порядок важен. Галочку ставить только после проверки, которая написана рядом.

Редакция 23.09.2026. Всё, что помечено [ГРАБЛИ], проверено на реальной установке с нуля на Windows 11:
там это ломалось, и рядом написано, чем лечится. Не пропускать, иначе споткнёшься на том же месте.


ШАГ 0. WINDOWS
  [ ] Часы синхронизированы, часовой пояс Астана (UTC+5).
  [ ] PowerShell один раз:   Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
  [ ] Папка:                 New-Item -ItemType Directory -Force C:\hack
      Путь без кириллицы и пробелов.


ШАГ 0.5. POWERSHELL 7 — ОБЯЗАТЕЛЬНО, НЕ ПРОПУСКАТЬ
  [ ] winget install --id Microsoft.PowerShell --source winget --accept-package-agreements --accept-source-agreements
      Проверка (в новом окне терминала):   pwsh -v   -> PowerShell 7.x

  [ГРАБЛИ] Зачем. Встроенный в Windows PowerShell 5.1 читает .ps1 без BOM как ANSI, а install.ps1 в UTF-8
  и с кириллицей внутри. Итог: скрипт падает с "The string is missing the terminator" и НЕ ставит скиллы.
  Симптом легко принять за сломанный комплект. На самом деле сломана кодировка.
  Лечение: всегда запускать скрипты комплекта через pwsh (см. ШАГ 5), а не двойным кликом и не в PowerShell 5.1.


ШАГ 1. NODE И PNPM
  [ ] Node.js 22 LTS: https://nodejs.org , установщик по умолчанию.     Проверка: node -v -> v22.x

  [ ] corepack и pnpm 10. Открыть PowerShell ОТ ИМЕНИ АДМИНИСТРАТОРА и выполнить две строки:
        corepack enable
        corepack prepare pnpm@10.34.5 --activate
      Проверка (в обычном окне):   pnpm -v   -> 10.34.5

  [ГРАБЛИ] Два подвоха разом.
  1. corepack enable пишет шимы в C:\Program Files\nodejs и без прав администратора падает с
     "EPERM: operation not permitted, open 'C:\Program Files\nodejs\pnpx'". Нужно именно окно от администратора.
  2. В старой редакции стояло  corepack prepare pnpm@latest  . Сейчас latest это pnpm 12, а весь комплект
     и репетиционное приложение собраны на pnpm 10, lock-файлы от 12 не подойдут. Версию пиннуем руками.

  [ ] Проверка, что не мешает лишний пакет:   npm ls -g --depth=0
      Если в списке есть строка  node@<версия>  — удалить:   npm rm -g node
  [ГРАБЛИ] Глобально установленный npm-пакет "node" (обёртка node-bin-gen) перекрывает настоящий Node.
  В git-bash это выглядит как  "This: command not found"  при запуске pnpm, vercel, codex и claude.
  В PowerShell при этом всё работает, поэтому причину ищут долго.

  [ ] npm НЕ обновлять командой  npm i -g npm@latest .
  [ГРАБЛИ] Текущий npm 12 требует Node >= 22.22.2. На Node 22.20 он откажется ставиться с ошибкой notsup.
  Если обновление нужно:   npm i -g npm@11


ШАГ 2. GIT ПОД СВОИМ ИМЕНЕМ (это важно: коммиты должны быть твои)
  [ ] Git for Windows: https://gitforwindows.org , установщик по умолчанию.
  [ ] git config --global user.name "Эмина Ануарбекова"
      git config --global user.email "почта твоего аккаунта GitHub"
      Твой аккаунт GitHub: Emina-An. Почту взять на github.com > Settings > Emails: либо основная почта аккаунта, либо
      служебный адрес вида 12345678+Emina-An@users.noreply.github.com, который показан там же (цифры свои, не выдумывать).
      С любой другой почтой GitHub не засчитает коммиты твоему профилю, а вклад участника проверяют по профилю.
  [ ] Первый вход: любой git push в свой тестовый репозиторий на GitHub, в окне войти своим аккаунтом.
      Проверка: git config --global --list -> твои имя и почта.

  [ ] Проверить, какой аккаунт GitHub помнит Windows:
        cmdkey /list | Select-String "github"
      Должен быть твой. Если чужой — удалить и зайти заново:
        cmdkey /delete:LegacyGeneric:target=git:https://github.com
      после этого следующая операция с GitHub откроет окно входа.

  [ГРАБЛИ] Если Windows помнит ЧУЖОЙ аккаунт GitHub, приватный репозиторий команды отдаёт
  "remote: Repository not found", а НЕ "403" и НЕ "denied". Выглядит так, будто репозитория не существует
  или ссылка неверная. На самом деле это отказ в доступе. В старой редакции инструкции был описан только
  симптом 403 — его можно не дождаться.


ШАГ 3. ПРАВО ЗАПИСИ В РЕПОЗИТОРИЙ КОМАНДЫ
  [ ] Открыть под своим аккаунтом: https://github.com/BAITC-Hacks/hack-463fe33c-lomra
      На README виден карандаш "Edit" и кнопка "Add file"  -> право есть.
      Не видно -> сразу написать организаторам (team@baitc.org или чат), чтобы добавили твой аккаунт.
  [ ] cd C:\hack
      git clone https://github.com/BAITC-Hacks/hack-463fe33c-lomra lomra
      Только клон. Ничего не отправлять до 13:00 23.09.
  [ ] Проверка права записи и входа в GitHub с этого ноутбука, без отправки чего-либо:
      cd C:\hack\lomra
      git push --dry-run origin main
      Ответ "Everything up-to-date" -> право есть, вход под твоим аккаунтом работает.
      Ответ "Repository not found" -> Windows помнит чужой аккаунт, см. ГРАБЛИ в ШАГ 2.
      Ответ с "denied" или 403 -> входишь под собой, но права нет: писать организаторам.


ШАГ 4. АГЕНТ (Codex)
  [ ] npm i -g @openai/codex          Проверка: codex --version
  [ ] codex login  -> свой аккаунт ChatGPT. Если подписки нет, этот шаг делается 23.09 в 12:30 с перк-аккаунтом.
      Проверка входа:  файл C:\Users\<ты>\.codex\auth.json существует.
  Запасной вариант вместо Codex: Claude Code (npm i -g @anthropic-ai/claude-code) со своим аккаунтом Claude, если он есть.
      Для Claude Code вход отдельный:  claude auth login  , проверка:  claude auth status  -> "loggedIn": true
      (вход в приложение Claude НЕ логинит CLI, это разные токены)

  [ ] ЗАПУСКАТЬ CODEX ТОЛЬКО ТАК:
        cd C:\hack\lomra
        codex --sandbox danger-full-access --ask-for-approval never

  [ГРАБЛИ] САМОЕ ВАЖНОЕ НА ЭТОЙ СТРАНИЦЕ. В старой редакции было  codex --full-auto . С этим флагом
  твой агент НЕ СМОЖЕТ КОММИТИТЬ ВООБЩЕ. Песочница Codex на Windows выполняет команды от отдельного
  пользователя (CodexSandboxOnline), и запись в папку .git она блокирует на своём уровне. Симптом:
        error: cannot open '.git/FETCH_HEAD': Permission denied
  При этом  git status  работает, поэтому кажется, что с git всё в порядке.
  Для тебя это смертельно: task, progress, readme, submit — все твои команды заканчиваются коммитом и пушем.
  Проверено: правами доступа на папку и настройкой writable_roots это НЕ лечится, только сменой режима песочницы.
  Эквивалент той же команды:  codex --dangerously-bypass-approvals-and-sandbox

  [ ] Заодно, один раз, окно администратора (убирает предупреждение "dubious ownership" у песочницы):
        git config --system --add safe.directory C:/hack/lomra
        git config --system --add safe.directory C:/hack/agent-harness


ШАГ 5. КОМПЛЕКТ КОМАНДЫ
  [ ] cd C:\hack
      git clone https://github.com/Alexanderadon/agent-harness
      pwsh -File C:/hack/agent-harness/install.ps1 -Role emina
      Проверка: строки "skill: ..." (десять штук) и "role (emina): ...".

  [ГРАБЛИ] Именно  pwsh -File , а не  .\install.ps1  — причина в ШАГ 0.5.
  Это же относится к повторному запуску после  git pull  комплекта.
  Проверить, что кириллица не побилась:  открыть C:\Users\<ты>\.codex\AGENTS.md — там должно читаться
  "Роль этого ноутбука на хакатоне: Эмина, капитан". Если видишь "Р РѕР»СЊ" — скрипт запускали не через pwsh.
  (Внимание: если смотреть файл командой Get-Content в PowerShell 5.1, кракозябры покажутся даже у корректного
  файла. Смотреть в VS Code или Блокноте.)

  [ ] Проверка агента: cd C:\hack\lomra , запустить codex командой из ШАГ 4, написать   Я Эмина
      Ответ: четыре строки, роль "капитан", твоё git-имя, ветка, команды task/вопросы/ответы/progress/readme/testdata/audit/submit.
      Если Codex не видит скилл: первым сообщением вставить текст файла C:\hack\agent-harness\skills\captain\SKILL.md, потом  Я Эмина.
  [ ] Пробная команда в любом СВОЁМ тестовом репозитории (не в lomra!):  progress
      -> строка в PROGRESS.md и пуш прошёл. Это единственная проверка, что твой агент умеет коммитить.


ШАГ 6. ПУТЬ СУДЬИ, ОДИН РАЗ РУКАМИ
  [ ] На любом проекте с GitHub:  git clone ...  ,  pnpm install  ,  pnpm dev  , открыть http://localhost:3000
      Смысл: 23.09 в 4:15 ты повторишь это с репозиторием команды строго по README, без подсказок Александра.
      По новой редакции Положения (п. 5.4.16 и 5.6.5) это не перестраховка: если проект не запускается
      по README, команду СНИМАЮТ С ОТБОРА, пояснения и исправления не принимаются.


ШАГ 7. ОСТАЛЬНОЕ
  [ ] VS Code (читать README и SPEC). Chrome или Edge. Telegram Desktop.
  [ ] Прочитать из комплекта: playbooks\hackalem-2026\PICK-TASK.md (выбор задачи), guide\03 Эмина.md (твой день) и guide\06 Как писать агенту.md (команды и что отвечать агенту).
  [ ] Телефон: чат хакатона (ссылка-приглашение в «ЧИТАТЬ ПЕРВЫМ.txt» и в твоём Telegram), почта, инкогнито-вкладка.
  НЕ ставить: Docker, Python, Vercel, Turso. Это не твоя часть.


В ДЕНЬ, 23.09 (старт 13:00, окончание 18:00)
  12:30  перки в кабинете: ключ API в менеджер паролей (свой и запасной), подписка Codex активирована.
  12:35  если Codex не проверялся раньше: codex login перк-аккаунтом, в C:\hack\lomra:  Я Эмина ,
         потом в тестовой папке  progress . До старта в lomra ничего не коммитить.
  12:40  методика оценки и порядок отбора: прочитать первой, Александру три вещи: условия допуска, материалы, дедлайн.
  13:00  своему агенту:  Я Эмина, старт 13:00 .  Тексты задач в один файл, промпт из PICK-TASK.md в свой ChatGPT,
         рейтинг Александру. ПЕРВЫМ ДЕЛОМ прочитать критерии оценки выбранной задачи в её ТЗ и пересказать
         Александру за две минуты: сколько баллов за что. Единой рубрики у хакатона больше нет (Положение 5.5).
  ~13:12 агенту:  task  и вставить текст выбранной задачи. ТОЛЬКО после того, как Александр сказал,
         что scaffold запушен: скаффолд занимает 9–12 минут, и твой коммит до него создаст лишнее слияние.
  Дальше по страницам "03 Эмина" и "06 Как писать агенту".


ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ, БЫСТРАЯ ТАБЛИЦА
  install.ps1 падает "string is missing the terminator"   -> запускать через pwsh (ШАГ 0.5)
  corepack enable: EPERM ... Program Files\nodejs\pnpx    -> окно администратора (ШАГ 1)
  pnpm -v показывает 12.x                                 -> corepack prepare pnpm@10.34.5 --activate
  git: "Repository not found" на репозитории команды      -> Windows помнит чужой GitHub (ШАГ 2)
  агент: cannot open '.git/FETCH_HEAD': Permission denied  -> запускать codex с --sandbox danger-full-access (ШАГ 4)
  в git-bash "This: command not found"                     -> npm rm -g node (ШАГ 1)
  npm i -g npm@latest: notsup, требует Node >= 22.22.2     -> npm i -g npm@11
  в AGENTS.md кракозябры "Р РѕР»СЊ"                        -> install.ps1 запускали не через pwsh, повторить

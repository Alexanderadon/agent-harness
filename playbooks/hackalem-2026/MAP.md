# HackAlem · карта действий простым языком

Один сценарий, один агент, один README. Всё остальное это шум. Единой рубрики нет: баллы ставятся по критериям из ТЗ выбранной задачи (п. 5.5, `docs/CRITERIA.md`); решают технические эксперты, AI-судья вспомогательный (п. 1.17). Ворота допуска общие: проект запускается по README (п. 5.4.16), ключевая функциональность проверяется без наших аккаунтов (п. 5.6.6), в README восемь разделов п. 5.4.15.

## Накануне, 22.09
1. Ноутбук по `LAPTOP.md`: все галочки, без «потом».
2. `agent-harness` запушен на GitHub, на ноутбуке выполнен `pwsh -File C:/hack/agent-harness/install.ps1 -Role alexander`.
3. Логины Vercel, Turso (app.turso.tech, вход через GitHub) и platform.openai.com живые на ноутбуке. Базы, проект lomra-demo и проект Vercel — утром по порядку ниже.
4. `pwsh -File C:/hack/agent-harness/prewarm.ps1`: все пакеты дня в кэше, скаффолд и сборка проверены.
5. Эмина: один раз нажала «Edit» и «Commit changes» на тестовом репозитории, шаблоны открыты, Telegram-канал хакатона на телефоне.
6. Спать.

## Утро 23.09, до старта
7. Всё до 12:30, строго по порядку (команды в `laptops/ALEXANDER-SETUP.txt`): Turso — две базы `lomra-probe` и `lomra`, токены Expiration: Never → OpenAI — проект lomra-demo, предоплата $15–20, автопополнение выключено → Vercel — проект `lomra` с env (ключ lomra-demo, `TURSO_*` базы `lomra`, `AGENT_MODEL`), Deployment Protection выключена → боевой прогон на копии rehearsal с probe-проектом Vercel (живой Run со стримингом, Turso, нет ENOENT schema.sql, время сборки) → параллельно ноутбук Эмины по EMINA-SETUP → проверка часов → тег `pre-start-2026-09-23` запушен. `TURSO_*` никогда не кладутся в `.env.local`.
8. 12:30 перки: ключ API уходит Эмине в менеджер паролей, Codex логинится в перк-аккаунт. Перк-ключ только в `.env.local`, в Vercel никогда.
9. Эмина читает методику оценки и порядок отбора, пересказывает три вещи: условия допуска, требуемые материалы, точный дедлайн.
10. `git clone` репозитория команды. Claude Code открыт в папке. Ничего не пушить. Тексты задач до 13:00 — только в `C:\hack\pre\ALL-TASKS.md`, вне репозитория команды; от Эмины через Telegram. Рейтинг по `PICK-TASK.md` во второй сессии Claude в папке agent-harness.
11. 12:50. Часы отдельной вкладкой терминала: `pwsh -NoProfile -File C:\hack\agent-harness\hack-clock.ps1 -Start 13:00`. Они считают блоки от старта, на :50 кричат «чекпоинт» (последний раз в 4:40), предупреждают про 3:30, 4:00 и 4:30. Агенту первое сообщение `Я Александр`: он подтверждает роль, показывает команды и отдельной строкой результат pull. Эмина на своём ноутбуке пишет своему `Я Эмина`.

## 0:00–0:22 · скаффолд, выбор, SPEC
12. 0:00. Пишешь `scaffold`. Он создаёт проект, раскладывает комплект, коммитит «scaffold + conventions (disclosed, agent-harness@<hash>)» и пушит. Работает само 9–12 минут (блок 0, 0:00–0:12).
13. Если задача не выбрана до 13:00: пока идёт скаффолд, Эмина собирает тексты задач в `C:\hack\pre\ALL-TASKS.md` и шлёт тебе в Telegram, ты прогоняешь их через промпт из `PICK-TASK.md`. Берёте первую по рейтингу без красных флагов. Смена трека в кабинете, если нужна. Спор дольше 10 минут запрещён.
14. ~0:05–0:10, сразу как задача выбрана, не дожидаясь скаффолда: Эмина делает `task` — условие и критерии дословно в `docs/TASK.md` и `docs/CRITERIA.md`. ~0:12–0:20 ты пишешь `spec`: агент сам пишет `SPEC.md`. Читаешь две минуты, правишь руками. Эмина сверяет таблицу требований с условием.
15. ~0:22. Пишешь `старт`. Агент пересказывает план одним абзацем и называет незакрытые требования. Пишешь `го`. Дальше `блок 3`, `блок 4` и так далее; в :50 `чекпоинт` и сразу деплой.

**Всё разом или по плану.** Контекст даёшь весь сразу: четыре файла, агент читает их до первой строки кода. Исполнение только по блокам, один за раз. Промпт «сделай всё» даёт наполовину рабочее целое, которое за 5 часов не отладить, и нечего показать на часовом чекпоинте. Блок даёт коммит, отчёт и точку, где можно резать.

## Блоки, по одному
| # | Что | Когда | Готово, когда |
|---|---|---|---|
| 0 | Скаффолд, комплект, первый пуш | 0:00–0:12 | typecheck зелёный, дефолтная страница открывается |
| 1 | База, сид, таблица, Reset, `/api/health` | 0:22–0:50 | таблица из сида видна, Reset возвращает сид, `/api/health` отвечает JSON |
| 2 | Пустой деплой (руками, параллельно; проект Vercel и env с утра) | ~0:25 | URL открывается с телефона в инкогнито без входа Vercel; `/api/health` ещё нет, с деплоя 0:50 ворота `"db":"turso","db_ok":true` |
| 3 | Чтение агентом, маршрут `/api/chat`, панель с трейсом | 0:50–1:40 | Run показывает вызовы в трейсе |
| 4 | Запись с Approve, сценарий целиком, потом `ревью` | 1:40–2:30 | Run, Approve, строка меняется; нарушения групп 1–2 починены |
| 5 | Валидация, ошибки, лимиты, без ключа | 2:30–2:50 | мусор даёт 400, без ключа есть уведомление |
| 6 | Тесты и `pnpm verify` | 2:50–3:15 | test зелёный без ключа, verify печатает PASS |
| 7 | Деплой-кандидат, смоук с телефона (руками) | 3:15–3:30 | `/api/health` → turso; Reset, Run, Approve на публичном URL в инкогнито |
| Э4 | README по критериям ТЗ с реальными выводами (Эмина: `readme` 1:30 и 3:15, `audit` 3:40) | 3:15–3:50 | восемь разделов п. 5.4.15, таблица требований без дыр |
| 9 | Дизайн агентом, не больше 15 минут, скриншот; режется первым, если дизайна нет в критериях | 3:15–3:30 | токены применены, 390px без скролла вбок |
| 10 | `ревью`, security-pass | 3:30–3:55 | ревью без нарушений групп 1–2, security-pass без находок |

Между блоками: `/clear`, коммит, отчёт агента в пять строк. У Эмины свой агент на её ноутбуке со скиллом `captain`: `task` в ~0:05–0:10 (до скаффолда можно), `readme` в 1:30 и 3:15, `судья сборка` в 2:00, `audit` в 3:40, `судья` в 4:00, `submit` не позже 4:30, `progress` в :55 каждого часа и последний в 4:35. Он коммитит под её аккаунтом, файлы у агентов разные, конфликтов нет, доска общая в TASKS.md. Страница Эмины: `EMINA.md`.
Чекпоинты 0:50, 1:50, 2:50, 3:50, 4:40. Деплой сразу после чекпоинтов 0:50, 1:50, 2:50, деплой-кандидат 3:15 (блок 7) и финальный в 4:00 — только при пустом `git status --short` и HEAD = origin/main (vercel заливает рабочую папку, а не коммит).
Параллельно Codex: тесты в 2:50, если Claude занят.

## Пять правил, которые не нарушаются
1. Один сценарий. Фича вне критериев ТЗ запрещена всегда; после 3:30 новое не начинается вообще.
2. Ни одного закэшированного ответа модели в приложении. Мок только в тестах.
3. Код только в репозитории платформы, пуш каждый час.
4. README пишется параллельно с 1:30, не в конце.
5. Дизайн не больше 15 минут в 3:15–3:30, ни минутой раньше; режется первым, если дизайна нет в критериях задачи.

## Проверки 1:40 и 2:30: отстаём?
- 1:40: Run показывает трейс? 2:30: Run → Approve → строка изменилась? Нет — отстаём.
- Сценарий не проходит: упростить цель, уменьшить сид, убрать второй инструмент чтения.
- **Отстаём на час** (1:40 трейса нет; блок 3 закончится ~2:40): режем блок 9 целиком (если дизайн в критериях — 10 минут), `ревью` в 2:30, второй инструмент чтения, баннер dirty. Блок 4 в 2:40–3:20, один инструмент записи с Approve. Блоки 5+6 одним блоком «доказательства» в 3:20–3:50: zod на /api/chat и /api/reset, 503 без ключа, takeRun из скилла как есть, tests/route.test.ts (400, 503), один тест инструмента на мок-модели, scripts/verify.ts и docs/verify.log. Деплой-кандидат 3:50, блок 10 — 10 минут чеклиста security-pass. Модель в Vercel gpt-6-luna без замера.
- **Отстаём на 2 часа** (2:30 трейса нет): в 2:30 блоки 3+4 сливаются в один «сценарий целиком» до 3:30 — одно чтение, одна запись с Approve, сид до 10 строк, цель по умолчанию из SPEC; в 3:30–3:50 только verify и route-тест; дизайна и ревью нет, security-pass 10 минут в 3:50; README Эмины честно пишет ограничения.
- Блоки «доказательств» (verify, тесты, zod) — не новое, правило 3:30 на них не действует.
- Не резать никогда (ворота допуска): запуск по README в чистом клоне, живой URL вместе с Turso (health: turso, db_ok), README по п. 5.4.15, `pnpm verify`, подтверждение на записи, пуш каждый час, судья, анкета до 4:30, ключ lomra-demo в Vercel. 3:30, 4:00 и 4:30 не сдвигаются.

## Финал 3:30–4:45
- 3:30 новое не начинать: ревью, security-pass (блок 10) и баги можно.
- 4:00 КОД ЗАМОРОЖЕН. Финальный деплой (пустой `git status --short`, HEAD = origin/main, `/api/health` → turso). Эмина: `судья` — её агент идёт по README буквально в чистом клоне; потом она руками `pnpm start` и живой URL в инкогнито.
- 4:00–4:25 только баги, каждый фикс сразу пуш и деплой по тому же правилу; после 4:25 код не коммитить.
- Модель в Vercel выбрана на деплое 2:50 и больше не меняется.
- Эмина: анкета отправлена не позже 4:30.
- 4:30 ДЕПЛОЙ ЗАМОРОЖЕН: дальше только `vercel rollback`.
- 4:35 последний `progress` Эмины. 4:40 твой последний `чекпоинт`, хеш Эмине. 4:45 она сверяет хеш на GitHub с телефона. После 17:45 не пушить никогда.

## Честно про «100%»
Гарантии нет: 10 призов на 900 команд. Под контролем только технический балл, и этот план выжимает его до предела: запускается у судьи, README отвечает на каждый критерий, verify печатает PASS, модель живая. Команда с этим набором обходит большинство поля до того, как кто-то посмотрит на интерфейс.

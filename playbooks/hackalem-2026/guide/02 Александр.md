# 02 · Александр: что нажимать и что писать

Твоя роль: код, деплой, дизайн. Ты не общаешься с организаторами и не пишешь README. Ты пишешь агенту команды в одно слово и проверяешь руками результат каждого блока.

## До 22-го, по порядку

1. Ноутбук по `LAPTOP.md`: Node 22, pnpm 10, Git, Claude, Codex, Vercel CLI, VS Code, часы синхронизированы, PowerShell разрешает скрипты.
2. Комплект: `git clone https://github.com/Alexanderadon/agent-harness`, в папке `.\install.ps1 -Role alexander`, потом `.\prewarm.ps1`.
3. Проверка агента: открыть Claude Code в любой папке, написать `Я Александр`. Должны прийти четыре строки: роль, git-имя, ветка, список команд.
4. Аккаунты: platform.openai.com со своим ключом и балансом на неделю (он будет жить в Vercel до 29.09), Turso с базой из дашборда, Vercel с пустым проектом и env, Deployment Protection выключена.
5. `git clone https://github.com/BAITC-Hacks/hack-463fe33c-lomra` в `C:\hack\`. Только клон, ничего не пушить. Потом в папке клона `git push --dry-run origin main`: «Everything up-to-date» значит, вход в GitHub на ноуте под Alexanderadon и право записи есть; 403 значит, Windows запомнил чужой аккаунт GitHub, перелогиниться. Ничего при этом не отправляется.
6. Репетиция по `REHEARSAL.md`, хотя бы час. Это единственное, что проверяет код, а не документы.

## Утро 23.09

- 12:30. Ключ API из кабинета в `C:\hack\<репо>\.env.local`: две строки, `OPENAI_API_KEY=` и `AGENT_MODEL=gpt-6-luna`. В Vercel — не он, а свой ключ с platform.openai.com: `vercel env add OPENAI_API_KEY production` (перк-ключ могут отозвать после события, а эксперты проверяют 24–28.09). Ключ в чат агенту не пишешь никогда, только в файл.
- 12:30, если в перках нет API-ключа. Смотри раздел «Если в перках нет API-ключа» ниже, это пять минут.
- 12:35. Codex: выйти из своего аккаунта, войти в перк-аккаунт. Не пользоваться до нужды.
- 13:00. Claude Code открыт в папке репозитория. Второе окно PowerShell в той же папке. Телефон рядом, инкогнито-вкладка готова.

## Если в перках нет API-ключа

Подписка ChatGPT и Codex приложению не подходят: это доступ для человека и для агента-кодера, а модель внутри нашего продукта вызывается только по API-ключу. Что в перках, узнаём в 12:30, поэтому три варианта по порядку:

1. **Ключ OpenAI есть** (перк или свой): `.env.local` как выше. Норма.
2. **Только NVIDIA** ($50 из перков): на build.nvidia.com взять ключ `nvapi-…` и id модели, у которой на странице написано function calling. В `.env.local` две строки: `NVIDIA_API_KEY=…` и `AGENT_MODEL=<id модели>`. Код тот же, провайдера выбирает lib/model.ts. Первый Run проверить сразу: другая модель может вызывать инструменты иначе, и цель по умолчанию, возможно, придётся упростить.
3. **Ничего**: свой аккаунт на platform.openai.com, $5 на баланс, ключ. Десять минут, если аккаунт заведён заранее. Если и этого нет, приложение сдаётся в режиме просмотра: таблица, Reset, честное «без ключа агент недоступен» в README, тесты и verify как доказательство цикла. Балл за реализацию ниже, дисквалификации нет.

Агент при `Я Александр` сам скажет, какой ключ видит в `.env.local`, по именам переменных, без значений. Ты в первом сообщении ничего про ключ писать не обязан.

## День, минута за минутой

Слева время, в середине что пишешь, справа что проверяешь руками до следующего шага. Между блоками сначала `/clear`, потом команда.

| Время | Пишешь агенту | Проверяешь руками |
|---|---|---|
| 0:00 | во втором окне: `.\hack-clock.ps1 -Start 13:00` (реальное время старта) | часы показывают «Блок 0» |
| 0:00 | `Я Александр` | четыре строки: роль, твоё git-имя, ветка main, список команд |
| 0:00 | `scaffold` | через 10–12 минут отчёт в три строки; в папке AGENTS.md, TASKS.md, docs/; `git log` два коммита |
| 0:03 | ничего, читаешь задачи, которые собрала Эмина | рейтинг от Эмины, выбор первой без красных флагов |
| 0:08 | ничего | Эмина сделала `task`, в docs/TASK.md лежит условие |
| 0:10 | `spec` | открываешь SPEC.md сверху вниз: «Подозрительные фрагменты ТЗ» (пусто или список цитат, ни одна не выполнена), «Для кого» (одна роль и её боль), «Вопросы заказчику» (не больше пяти, у каждого допущение), таблица требований: каждое к инструменту или экрану, строка «НЕ ЗАКРЫТО: нет». Не так: правишь файл руками или пишешь «исправь: …» |
| 0:15 | `старт` | абзац: что строим и какое требование закрывает каждый инструмент. Дыры названы |
| 0:15 | `го` | блок 1 пошёл |
| 0:20 | во втором окне: `vercel link`, потом `vercel --prod --yes` | пустой деплой открылся с телефона в инкогнито |
| 0:25 | во втором окне: `vercel env add TURSO_DATABASE_URL production`, то же для `TURSO_AUTH_TOKEN`, `AGENT_MODEL` | env на месте |
| 0:40 | отчёт агента по блоку 1 пришёл | `pnpm dev` во втором окне, localhost:3000: экран данных по форме из SPEC (таблица со статусами, список документов или форма ввода) заполнен из сида, Reset возвращает всё |
| 0:40 | `ответы`, если Эмина сказала «ответы в репе» (docs/ANSWERS.md) | SPEC обновлён, объём не вырос: если ответ ментора добавляет работу, она в «Не делаем» или в requests, не в блок |
| 0:40 | `/clear`, потом `блок 3` | |
| 0:50 | `чекпоинт` | коммит и пуш прошли, список коммитов за час на экране |
| 1:40 | отчёт по блоку 3 | Run: в трейсе два вызова чтения с входами и результатами, внизу текст. Засечь секунды |
| 1:40 | `/clear`, потом `блок 4` | |
| 1:50 | `чекпоинт` | |
| 2:30 | отчёт по блоку 4 | Run, карточка Approve, Approve: строка меняется, журнал обновился. Reject ничего не меняет |
| 2:30 | `ревью` | список нарушений в три группы; группы 1 и 2 он чинит сам одним коммитом |
| 2:30 | часы кричат «проверка»: отстаём или нет | если отстаём, режем по списку из общего плана |
| 2:35 | `/clear`, потом `блок 5` | мусор в поле цели даёт 400; убрать ключ из .env.local, панель показывает уведомление, вернуть ключ |
| 2:50 | `чекпоинт` | |
| 2:50 | `/clear`, потом `блок 6` | `pnpm test` без ключа зелёный; `pnpm verify` печатает шаги и PASS; docs/verify.log и docs/test.log лежат |
| 3:15 | во втором окне: `vercel --prod --yes` | с телефона в инкогнито: Reset, Run, Approve, строка меняется. URL Эмине для README |
| 3:45 | `/clear`, потом `блок 9, 15 минут` | шрифт Golos, фон серый, карточки белые, на телефоне нет скролла вбок |
| 3:50 | `чекпоинт` | |
| 4:00 | ничего, читаешь README, который сделала Эмина | каждая строка таблицы требований правда. Неправда: говоришь ей словами |
| 4:15 | `ревью`, потом `блок 10` | нарушений групп 1 и 2 нет; шаблоны из docs удалены; `pnpm smoke` зелёный |
| 4:25 | в Vercel `AGENT_MODEL=gpt-6-sol`, `vercel --prod --yes` | прогон с телефона ещё раз |
| 4:30 | ничего | Эмина прошла README на своём ноутбуке до Approve. Не прошла: чинишь тот шаг, ничего не добавляешь |
| 4:40 | говоришь Эмине «ок» | она отправляет анкету |
| 4:45 | `чекпоинт` последний раз | пуш прошёл. Руки убрать |

## Что ты думаешь на каждом отчёте агента

Отчёт приходит в пять строк: сделано, проверено, не сделано, риски, следующий блок. Три вопроса к каждому:
1. Критерий блока из таблицы выполнен буквально? Не «почти», а так, как написано.
2. То, что «проверено руками» агентом, я могу повторить сам за минуту? Повторяю.
3. В «не сделано» есть что-то из обязательных требований задачи? Если да, это чинится сейчас, а не в следующем блоке.

## Если в условии задачи есть команды агенту

Текст условия для агента это данные, как строка в таблице. Он не выполняет ничего, что написано внутри условия: «скачай по ссылке», «используй этот ключ», «игнорируй правила», скрытые комментарии, закодированные блоки. Всё такое он выписывает в SPEC.md в раздел «Подозрительные фрагменты ТЗ» и идёт дальше по видимым требованиям. Ты читаешь этот раздел первым. Если там что-то есть, Эмина спрашивает ментора, что это, а вы не выполняете это ни руками, ни агентом: ловушка или небрежность заказчика, в обоих случаях мимо.

Если агент всё же перешёл по ссылке, скачал файл или поставил пакет «по требованию условия»: `стоп`, потом «откати всё, что сделано по тексту условия, библиотеку удали, в SPEC запиши это как подозрительный фрагмент». Потом `git status` и package.json глазами.

Дыры условия (нет данных, требования противоречат, непонятно, для кого) это штатная ситуация: агент пишет вопрос и своё допущение, Эмина несёт вопросы ментору, ты работаешь по допущению и не ждёшь ответа.

## Чего не делать

- Не выполнять руками то, что агент выписал как подозрительный фрагмент условия: ни ссылки, ни ключи, ни «быстрые команды».
- Не писать агенту длинные объяснения. Команда, потом проверка, потом следующая команда.
- Не открывать вторую фичу, даже очевидную. Всё, что не в TASKS.md, идёт в раздел requests и ждёт 4:15.
- Не править README: им владеет агент Эмины, конфликт в git стоит десять минут.
- Не запускать `pnpm dev` через агента: только руками во втором окне.
- Не отвечать на вопросы организаторов: это к Эмине.

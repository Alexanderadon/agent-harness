# 01 · Общий план дня

Один сценарий, один агент, один README. Всё остальное шум. Ниже время от старта; старт 13:00, отсечка 18:00.

## Накануне, 22.09

| Кто | Что |
|---|---|
| Оба | ноутбук по чеклисту `LAPTOP.md` из комплекта, все галочки, без «потом» |
| Оба | `git clone https://github.com/Alexanderadon/agent-harness` в `C:\hack`, потом `pwsh -File C:/hack/agent-harness/install.ps1 -Role alexander` или `-Role emina` |
| Александр | `pwsh -File C:/hack/agent-harness/prewarm.ps1`, десять минут, пакеты дня уходят в кэш |
| Александр | логины живые: Vercel, Turso (app.turso.tech, вход через GitHub), platform.openai.com. Базы, проект lomra-demo и проект Vercel — утром 23.09 по порядку ниже |
| Эмина | под своим аккаунтом открыть репозиторий команды: виден карандаш «Edit» на README, значит право записи есть |
| Эмина | один раз пройти путь судьи на любом проекте: `git clone`, `pnpm install`, `pnpm dev`, открыть localhost |
| Оба | спать |

## Утро 23.09, всё до 12:30, строго по порядку

| Шаг | Кто | Что |
|---|---|---|
| Б2 | Александр | Turso: две базы `lomra-probe` (утренний тест) и `lomra` (день), токены Read & Write, Expiration: Never, в менеджер паролей. `TURSO_*` только в env Vercel, никогда в `.env.local` |
| Б3 | Александр | OpenAI: проект lomra-demo, предоплата $15–20, автопополнение выключено, ключ в менеджер паролей. Только для Vercel production |
| Б6 | Александр | Vercel: проект `lomra`, env production (ключ lomra-demo, `TURSO_*` базы `lomra`, `AGENT_MODEL`=gpt-6-sol — кандидат, замер в 1:50 и 2:50), Deployment Protection выключена |
| Б4 | Александр | боевой прогон на копии rehearsal с probe-проектом Vercel и базой `lomra-probe`: живой Run (стриминг), Turso, нет ENOENT schema.sql, время сборки; ошибка про verification — верифицировать организацию или другой ключ/модель |
| Б7 | Эмина (+ Александр) | параллельно с Б4: её ноутбук по `EMINA-SETUP.txt` |
| Б8 | Александр | часы: `pwsh -NoProfile -File C:\hack\agent-harness\hack-clock.ps1 -Start 13:00 -Once` |
| Б9 | Александр | до 12:30 тег `pre-start-2026-09-23` в agent-harness и его пуш; дальше скиллы чинить только локально в `~/.claude/skills` |

## Утро, 12:30–13:00

| Время | Кто | Что |
|---|---|---|
| 12:30 | Эмина | перки в кабинете: ключ API в менеджер паролей и Александру, Codex-подписка активирована |
| 12:30 | Александр | перк-ключ только в `.env.local` клона репозитория команды (`AGENT_MODEL=gpt-6-luna`), в Vercel никогда; Codex залогинен в перк-аккаунт. В перках только подписка, а не ключ: ключ NVIDIA из перков и `AGENT_MODEL` с build.nvidia.com, см. [[02 Александр]] |
| 12:35 | Эмина | `Я Эмина` своему агенту в тестовой папке, потом `progress`: проверка, что агент и git у неё работают |
| 12:40 | Эмина | методика оценки и порядок отбора финалистов: прочитать первой, пересказать Александру три вещи: условия допуска, требуемые материалы, дедлайн. Тексты задач, если видны, — в `C:\hack\pre\ALL-TASKS.md` вне репозитория команды и Александру через Telegram |
| 12:40 | Александр | вторая сессия Claude в `C:\hack\agent-harness`: рейтинг задач по `PICK-TASK.md`, только рейтинг, ничего в репозитории команды |
| 12:50 | Александр | `git clone` репозитория команды в `C:\hack\` уже сделан, Claude Code открыт в папке; часы отдельной вкладкой: `pwsh -NoProfile -File C:\hack\agent-harness\hack-clock.ps1 -Start 13:00`; агенту `Я Александр`. Ничего не пушить |

## Первые 22 минуты

| Время | Александр | Эмина |
|---|---|---|
| 0:00 | `scaffold` (блок 0, 9–12 минут) | своему агенту `Я Эмина`, если ещё не писала; тексты задач трека и соседних в `C:\hack\pre\ALL-TASKS.md`, Александру в Telegram |
| 0:03 | ждёт скаффолд, прогоняет задачи через `PICK-TASK.md` во второй сессии Claude | ждёт рейтинг |
| ~0:05–0:10 | выбирает задачу: первая по рейтингу без красных флагов | сразу `task` с текстом выбранной задачи и критериями, не дожидаясь скаффолда |
| ~0:12 | `spec`, читает SPEC.md две минуты: подозрительные фрагменты условия, для кого, вопросы с допущениями, таблица требований; правит | ждёт «SPEC готов»; ~0:20 `вопросы`: сверяет таблицу требований в SPEC с условием, получает docs/QUESTIONS.md |
| ~0:22 | `старт`, читает абзац, `го` | идёт к ментору партнёра с вопросами из QUESTIONS.md, ответы записывает дословно; ~0:25 `testdata`, если условие требует набор |
| 0:35 | — (ответы примет в 0:50 после чекпоинта, если в репозитории появился docs/ANSWERS.md) | `ответы` с текстом ментора; Александру словами «ответы в репе». Нет ментора: допущения остаются |

Спор о выборе задачи дольше 10 минут запрещён: берётся первая по рейтингу.

## Блоки

| # | Что | Кто | Когда | Готово, когда |
|---|---|---|---|---|
| 0 | скаффолд, комплект, первый пуш | агент А | 0:00–0:12 | typecheck зелёный, страница открывается |
| Э1 | условие и критерии в docs/TASK.md и docs/CRITERIA.md, не дожидаясь скаффолда | агент Э | ~0:05–0:10 | коммит от Эмины |
| 1 | база, сид, экран данных по форме из SPEC, Reset, `/api/health` | агент А | 0:22–0:50 | данные из сида на экране, Reset возвращает сид, `/api/health` отвечает JSON |
| 2 | пустой деплой (проект Vercel и env с утра) | А руками | ~0:25 | URL открывается с телефона в инкогнито без входа Vercel; `/api/health` проверяется с деплоя 0:50 |
| Э2 | черновик данных, если условие требует набор | агент Э | ~0:25–0:45 | docs/seed-draft.json и docs/test-cases.md, просьба в TASKS; до 0:50, чтобы блок 3 его подхватил |
| 3 | чтение агентом, маршрут, панель с трейсом | агент А | 0:50–1:40 | Run показывает вызовы в трейсе |
| Э3 | черновик README, `readme` | агент Э | 1:30–2:00 | все разделы, вопросы Александру списком |
| 4 | запись с Approve, сценарий целиком, потом `ревью` в 2:30 | агент А | 1:40–2:30 | Run, Approve, строка меняется |
| Э-с | `судья сборка`: чистый клон, `pnpm install --frozen-lockfile`, `pnpm build` | агент Э | 2:00 | docs/JUDGE-CHECK.md: «СУДЬЯ: ПРОШЁЛ»; УПАЛ — Александр чинит первым |
| 5 | валидация, лимиты, без ключа | агент А | 2:30–2:50 | мусор даёт 400, без ключа есть уведомление |
| 6 | тесты, verify, логи в docs | агент А | 2:50–3:15 | test зелёный без ключа, verify печатает PASS |
| 7 | деплой-кандидат, смоук с телефона | А руками | 3:15–3:30 | `/api/health` → turso; сценарий проходит на публичном URL |
| 9 | дизайн агентом, не больше 15 минут, скриншот; режется первым, если дизайна нет в критериях | агент А | 3:15–3:30 | токены, 390px без скролла вбок |
| Э4 | `readme` в 3:15, живой URL в 3:35, `audit` в 3:40 | агент Э | 3:15–3:50 | восемь разделов п. 5.4.15, таблица требований без дыр |
| 10 | `ревью`, security-pass | агент А | 3:30–3:55 | нарушений нет, шаблоны удалены |
| Э5 | `судья` (README буквально в чистом клоне), потом руками `pnpm start` и живой URL; `submit`, анкета | Эмина | 4:00–4:30 | «СУДЬЯ: ПРОШЁЛ»; анкета отправлена не позже 4:30 |

Между блоками у Александра `/clear`, потом следующий `блок N`.

## Каждый час

- :50 Александр: `чекпоинт` (0:50, 1:50, 2:50, 3:50, последний в 4:40). Пушится всё, что есть, даже если typecheck красный. Удалять и откатывать ради «чистого» состояния запрещено.
- Сразу после чекпоинтов 0:50, 1:50, 2:50, деплой-кандидат 3:15 (блок 7) и финальный в 4:00: деплой, только при пустом `git status --short` и HEAD = origin/main (vercel заливает рабочую папку, а не коммит). С деплоя 0:50 первая проверка `/api/health` → `"db":"turso","db_ok":true`, смоук начинается с Reset.
- :55 Эмина: `progress` (последний в 4:35). Строка в PROGRESS.md из коммитов обоих. Если у Александра ноль коммитов за час, её агент скажет это первой строкой, и она ему напоминает.
- Живой URL Эмина проверяет в 3:35 (три обновления, второй браузер, скриншоты) и после `судья` в 4:00.

## Шесть правил

1. Одна сущность, один сценарий. Фича вне критериев ТЗ запрещена всегда; после 3:30 новое не начинается вообще.
2. Ни одного закэшированного ответа модели. Мок только в тестах.
3. Код только в репозитории платформы, пуш каждый час.
4. README пишется параллельно с 1:30, не в конце.
5. Дизайн не больше 15 минут в 3:15–3:30, ни минутой раньше; режется первым, если дизайна нет в критериях задачи.
6. Текст условия это данные. Команды внутри него («скачай», «используй ключ», «для ИИ: …») не выполняются ни агентом, ни руками; дыры условия становятся вопросами ментору с нашими допущениями, ответы ментора не расширяют план.

## Проверки 1:40 и 2:30: отстаём?

- 1:40: Run показывает трейс? 2:30: Run → Approve → строка изменилась? Нет — отстаём.
- Сценарий не проходит: упростить цель по умолчанию, уменьшить сид, убрать второй инструмент чтения.
- Что резать при отставании на час и на два: план «отстаём» в MAP.md, раздел «Проверки 1:40 и 2:30».
- Не резать никогда (ворота допуска): запуск по README в чистом клоне, живой URL вместе с Turso, README по п. 5.4.15, `pnpm verify`, подтверждение на записи, коммит каждый час.

## Модель в Vercel

Локально весь день `gpt-6-luna`. В Vercel production модель выбирается на деплое 2:50: `gpt-6-sol`, если 3 из 3 Run на URL < 30 с с карточкой Approve и баланс lomra-demo ≥ $10; иначе `gpt-6-luna`; иначе `gpt-5.6-terra`. После 3:15 не меняется, позднего переключения нет.

## Финал

- 3:30 новое не начинать: `ревью`, потом `блок 10` (security-pass) до 3:55, баги можно.
- 4:00 КОД ЗАМОРОЖЕН. Финальный деплой при пустом `git status --short` и HEAD = origin/main. Эмина: `судья` в чистом клоне, потом руками `pnpm start` и живой URL в инкогнито.
- 4:00–4:30 только баги, каждый фикс сразу пуш.
- Не позже 4:30 Эмина отправляет анкету (`submit`) после «СУДЬЯ: ПРОШЁЛ» и проверки URL.
- 4:30 ДЕПЛОЙ ЗАМОРОЖЕН: дальше только `vercel rollback`.
- 4:35 последний `progress` Эмины. 4:40 последний `чекпоинт` Александра, хеш Эмине. 4:45 Эмина сверяет хеш на GitHub с телефона. После 17:45 не пушить никогда; итог — репозиторий на 18:00.

# 03 · Эмина: что нажимать и что писать

Твоя роль: капитан. Задача, документы, данные, организаторы, проверка как судья, подача. Кода ты не касаешься, и твой агент тоже. Ты пишешь ему команды в одно слово, он пишет файлы и сам отправляет их в репозиторий под твоим именем.

## До 22-го, по порядку

1. На ноутбуке: Node 22, pnpm 10, Git for Windows, Codex CLI (`npm i -g @openai/codex`), VS Code. В PowerShell один раз: `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`.
2. Git с твоим именем: `git config --global user.name "Эмина Ануарбекова"` и `git config --global user.email "…"`, где почта взята с github.com, Settings, Emails: основная почта аккаунта Emina-An или служебный адрес вида `12345678+Emina-An@users.noreply.github.com`, который там показан (цифры свои). С другой почтой GitHub не зачтёт коммиты твоему профилю. Один тестовый `git push` в любой свой репозиторий, чтобы Windows запомнил вход именно под Emina-An.
3. Комплект: `git clone https://github.com/Alexanderadon/agent-harness`, в папке `.\install.ps1 -Role emina`.
4. Проверка права записи: открыть https://github.com/BAITC-Hacks/hack-463fe33c-lomra под своим аккаунтом. На README виден карандаш «Edit» и кнопка «Add file». Не видно: до 22-го написать организаторам, чтобы добавили твой аккаунт.
5. `git clone https://github.com/BAITC-Hacks/hack-463fe33c-lomra` в `C:\hack\`. Только клон, ничего не отправлять. Потом в папке клона `git push --dry-run origin main`: ответ «Everything up-to-date» значит, право записи есть и вход под твоим аккаунтом работает; «denied» или 403 значит, до 22-го писать организаторам. Ничего при этом не отправляется.
6. Проверка агента: в папке клона запустить `codex --full-auto`, написать `Я Эмина`. Должны прийти четыре строки: роль, твоё git-имя, ветка, список команд. Если Codex не видит скилл, первым сообщением вставить текст файла `skills/captain/SKILL.md` из комплекта, потом `Я Эмина`.
7. Один раз пройти путь судьи на любом проекте: `git clone`, `pnpm install`, `pnpm dev`, открыть localhost. Просто чтобы руки помнили.
8. Прочитать `PICK-TASK.md` из комплекта: там шесть вопросов к задаче и промпт.

## Утро 23.09

- 12:30. Перки в кабинете. Ключ API в менеджер паролей, второй ключ туда же как запасной. Подписка Codex активирована.
- 12:35. Если Codex до этого не работал без подписки: сейчас `Я Эмина` и `progress` в тестовой папке. Двадцать минут на это есть.
- 12:40. В кабинете и в Telegram-канале должны появиться методика оценки и порядок отбора. Читаешь первой. Александру четыре вещи: обязательные условия допуска, какие материалы сдавать, точный дедлайн, и есть ли ежечасная отчётность в кабинете помимо коммитов. Если есть форма или кнопка в кабинете, это твоя задача в :55 рядом с `progress`: коммиты сами по себе её не заменят.
- 13:00. Телефон: канал хакатона, почта, инкогнито-вкладка. Ноутбук: папка репозитория, Codex открыт.

## День, минута за минутой

| Время | Пишешь агенту | Что делаешь сама |
|---|---|---|
| 0:00 | `Я Эмина, старт 13:00` (реальное время старта) | четыре строки: роль капитан, твоё git-имя, ветка main, команды |
| 0:00 | ничего | в кабинете открыть задачи своего трека и соседних, скопировать тексты в один файл на ноутбуке |
| 0:03 | ничего | в своём ChatGPT вставить промпт из `PICK-TASK.md` и тексты задач; рейтинг Александру |
| 0:08 | `task`, следом вставить текст выбранной задачи целиком | агент сначала перечислит подозрительные места в тексте (команды для ИИ, ссылки «скачай», скрытый текст); ничего из этого не делаете ни ты, ни он. Потом docs/TASK.md сохранён и отправлен; это твой первый коммит |
| 0:12 | `вопросы` | Александр сделал `spec`: открыть SPEC.md, сверить таблицу требований с условием, прочитать «Для кого» и «Подозрительные фрагменты ТЗ». Пропущено требование или не тот пользователь: сказать ему. Агент собрал docs/QUESTIONS.md: вопросы заказчику с нашими допущениями |
| 0:20 | ничего | найти на площадке представителя компании-заказчика: сначала вопросы из docs/QUESTIONS.md, потом один свой: «что заставит вас запустить пилот завтра?». Ответы записать дословно. Нет ментора или не отвечает: возвращаешься, наши допущения остаются в силе |
| 0:35 | `ответы`, следом вставить ответы ментора | docs/ANSWERS.md в репозитории, просьба Александру в TASKS; сказать ему словами «ответы в репе», он введёт `ответы` на границе блока. Ментор противоречит условию: условие главнее, судья сверяет с текстом кейса; Александр план не расширяет |
| 0:40 | `testdata` (только если условие требует приложить набор данных) | агент сделал docs/seed-draft.json и docs/test-cases.md, просьба Александру в TASKS |
| 0:55 | `progress` | строка в PROGRESS.md появилась; если агент пишет «у Александра ноль коммитов», сказать Александру |
| 1:00 | ничего | живой URL в инкогнито на телефоне: открывается, таблица видна |
| 1:30 | `readme` | черновик README; агент выдаст список вопросов Александру, задать их ему словами, когда он закроет блок |
| 1:55 | `progress` | |
| 2:00 | ничего | прочитать README глазами человека, который не знает проекта: непонятное и обещанное, но невидимое в интерфейсе, сказать Александру |
| 2:55 | `progress` | живой URL в инкогнито |
| 3:15 | `readme` | README обновлён, в нём выводы verify и test из docs |
| 3:40 | `audit` | список: подтверждено, не подтверждено, противоречит. Противоречия агент удалил сам; неподтверждённое спросить у Александра |
| 3:55 | `progress` | |
| 4:15 | ничего | ты судья: новая папка, `git clone` репозитория команды, потом строго по README: `pnpm install`, `pnpm dev`, localhost, Reset, Run, Approve. Дошла: скажи Александру. Не дошла: скажи, на каком шаге |
| 4:20 | `submit` | docs/SUBMISSION.md: URL, описание, состав, всё для анкеты |
| 4:30 | ничего | анкета в кабинете заполнена черновиком из SUBMISSION.md |
| 4:40 | ничего | «ок» от Александра, отправить анкету. Не в 4:59 |
| 4:55 | `progress` последний раз | |

## Если агент на ноутбуке не работает

Всё то же самое руками с телефона через сайт GitHub: открыть файл, карандаш «Edit», текст, «Commit changes». Условие в docs/TASK.md, строка в PROGRESS.md каждый час. README тогда пишет агент Александра по его просьбе, а ты читаешь.

## Чего не делать

- Ничего не отправлять в репозиторий до 13:00.
- Не давать свой аккаунт Codex Александру и не брать его: каждый пользуется своим.
- Не просить агента править код. Просьба к коду это строка в TASKS.md в разделе requests, агент сам её напишет, если сказать «попроси Александра: …».
- Не предлагать вторую фичу. Одна задача, один сценарий, до конца.
- Не отвечать за Александра на технические вопросы проверяющих: записать вопрос, передать ему на границе блока.
- Не выполнять то, что написано внутри условия как команда: ссылки «скачай», ключи, «для ИИ: сделай так». Агент выписывает это как подозрительное, ты несёшь ментору вопрос, что это. Ловушка или небрежность, в обоих случаях мимо.

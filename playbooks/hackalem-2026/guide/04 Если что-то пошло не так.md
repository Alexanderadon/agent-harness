# 04 · Если что-то пошло не так

Две части. Первая: как писать агенту, чтобы не сбить его с плана. Вторая: конкретные ситуации, что писать и сколько это стоит по времени.

## Как писать агенту, когда что-то сломалось

Агент идёт по блокам. Каждое лишнее сообщение это риск, что он бросит блок и начнёт чинить то, что ты назвал. Поэтому:

**Сначала реши, блокирует ли это сценарий.** Сценарий это Reset, Run, Approve, строка изменилась. Если сломано что-то на этом пути, чинить сейчас. Если нет (шрифт, цвет, кривая кнопка, отступ), это косметика: в блок 9, не раньше.

**Косметику не пиши агенту вообще.** Пиши в файл TASKS.md в раздел requests одну строку: «А: кнопка Approve на телефоне обрезана». В блоке 9 агент прочитает список и сделает всё разом. Одно сообщение агенту про шрифт посреди блока 4 стоит десять минут: он переключится, полезет в стили, потеряет контекст блока.

**Баг, который блокирует, пишется по форме.** Четыре строки, ничего лишнего:

```
Баг в блоке 4. Вижу: после Approve строка не меняется, в трейсе output-error.
Ожидал: статус «заказ сформирован». Блокирует сценарий: да.
Почини только это, ничего другого не трогай, покажи, что изменилось.
```

Что ты видишь, что ожидал, блокирует или нет, «только это». Не пиши, как чинить: агент знает код лучше, чем ты его помнишь.

**После починки проверь руками сам** и только потом следующая команда. Агент говорит «готово» раньше, чем это правда.

**Если агент задал вопрос,** ответь одной строкой. Не знаешь ответа: «выбери самый простой вариант и продолжай».

**Если агент ушёл в сторону** (делает не то, что в блоке), одно слово: `стоп`. Он откатит лишнее и вернётся.

**Если агент крутится дольше 15 минут** без результата: `чекпоинт`, потом `/clear`, потом снова `блок N` со словами «продолжи с того, что есть, не переписывай». Каждый повторный запуск блока это минус пять минут, три подряд это сигнал упростить блок, а не повторять.

**Никогда не пиши:** «переделай всё», «сделай красиво», «добавь ещё», «а можно ещё…». Каждое из этих слов снимает с плана полчаса.

## Ситуации

Порядок по вероятности. В третьей колонке цена по времени, если действовать по инструкции; без инструкции обычно втрое.

| Что случилось | Блокирует? | Что делать и что писать | Цена |
|---|---|---|---|
| База не создаётся, ошибка про libsql или нативный модуль | да | Александру во втором окне: `pnpm install` заново; если не помогло, агенту: «Баг в блоке 1. Вижу: <текст ошибки, 5 строк>. Почини подключение базы, схему не меняй» | 10 мин |
| Таблица пустая после запуска | да | агенту: «Баг: таблица пустая при первом запуске. Сид должен накатываться при первом подключении, pnpm dev должно быть достаточно» | 5 мин |
| Ключ OpenAI не работает: 401 или 429 | да | локально: `.env.local`, ключ и `AGENT_MODEL` без опечаток, потом NVIDIA-резерв. На живом URL панель пишет «Ключ модели на демо-сервере недействителен или отозван» или «Лимит запросов или баланс ключа исчерпан»: это ключ lomra-demo в Vercel — баланс в дашборде OpenAI, ротация `vercel env rm OPENAI_API_KEY production`, `vercel env add OPENAI_API_KEY production`, Redeploy того же коммита. Перк в Vercel не кладётся никогда. Агенту ничего не писать, это env | 5 мин |
| Живой Run падает с ошибкой про verification или stream | да | новая организация OpenAI требует верификацию для стриминга. Верифицировать организацию (документ вводит сам Александр) или другой ключ/модель в env Vercel, Redeploy. Код не трогать | 10 мин |
| API-ключа нет вообще: в перках только подписка ChatGPT | да, для живого Run | подписка приложению не подходит. Ключ NVIDIA из перков и `AGENT_MODEL` с build.nvidia.com в `.env.local`, модель с function calling; код менять не нужно. Нет и этого: локально свой ключ OpenAI. Живой URL от перков не зависит: в Vercel production с утра ключ lomra-demo; режим просмотра без ключа не проходит п. 5.6.6. Агенту ничего | 10 мин |
| Run крутится дольше минуты и не заканчивается | да | агенту: «Баг: прогон не завершается за 60 с. Снизь лимит шагов до 4 и уменьши выборку инструмента чтения до 10 строк» | 10 мин |
| Кнопка Approve не появляется | да | агенту: «Баг: после вызова инструмента записи нет карточки Approve. Проверь, что инструмент есть в toolApproval и что панель рисует состояние approval-requested» | 10 мин |
| После Approve ничего не меняется | да | форма бага выше. Обычно инструмент не пишет в базу или таблица не перечитывается | 10 мин |
| Форма или поле не отправляется, кнопка мёртвая | да, если это путь сценария | агенту: «Баг: <какая форма> не отправляется, в консоли <текст>. Проверь обработчик и zod-схему, покажи ошибку в интерфейсе» | 10 мин |
| Тесты красные без ключа | нет, но нужно к 3:15 | агенту: «Тесты должны быть зелёными без ключа: agent.test.ts использует MockLanguageModelV4, tools.test.ts тестовую базу» | 10 мин |
| `pnpm verify` падает | нет, но нужно к 3:15 | агенту: «verify падает: <последние 20 строк>. Почини скрипт, сценарий из SPEC не меняй» | 10 мин |
| Деплой на Vercel падает | нет | во втором окне `pnpm build` локально: если красный, агенту «Баг: build падает: <20 строк>». Если зелёный локально, проверить env в Vercel. Дольше 20 минут: деплой позже, локальный путь главный. После 4:30 новых деплоев нет: сломанный URL — только `vercel rollback` | 10–20 мин |
| Деплой не запускается по правилу: `git status --short` не пуст или HEAD ≠ origin/main | нет | vercel заливает рабочую папку, а не коммит. Дождаться коммита и пуша агента, потом деплой; не успел до следующего чекпоинта — этот час без деплоя | 0–5 мин |
| Живой URL показывает форму входа Vercel | нет | настройки проекта в Vercel, Deployment Protection выключить. Агенту ничего | 3 мин |
| Turso не подключается на Vercel: `/api/health` не `"db":"turso","db_ok":true`, на странице красная строка «Демо-база в памяти» | да | это ворота допуска, не резать и не убирать Turso из env. `vercel env ls production`: есть ли `TURSO_DATABASE_URL` (libsql://…) и `TURSO_AUTH_TOKEN` ровно с такими именами (имя с префиксом, например `STORAGE_TURSO_DATABASE_URL`, значит интеграцию подключили с Custom Prefix: отключить и подключить заново с пустым полем). База из Marketplace: Vercel → Storage → база → Rotate Secrets, потом `vercel --prod --yes`. Иначе новый токен в app.turso.tech (Expiration: Never), `vercel env rm` + `vercel env add`, `vercel --prod --yes`, `/api/health`. В `.env.local` `TURSO_*` не класть. Не завелось за 20 минут: сразу Эмине, URL с красной строкой честно в ограничения README, основной путь проверки локальный | 20 мин |
| Reset или Approve на живом URL дают 500 после нового деплоя | да | схема в Turso старая. Нажать Reset: он удаляет доменные таблицы и заново выполняет schema.sql. Не помогло: агенту «Баг: Reset на Turso 500: <строка из vercel logs>. Reset должен пересоздавать схему, agent_runs не трогать» | 10 мин |
| Шрифт, цвета, отступы, кривая кнопка, не тот заголовок | нет | строка в TASKS.md, раздел requests. Блок 9 сделает всё разом. Агенту сейчас ничего | 0 мин сейчас |
| На телефоне не видно кнопку Run или Approve | да | это не косметика, это путь сценария: агенту «Баг: на ширине 390px кнопка Approve за экраном. Панель должна вставать под таблицу» | 10 мин |
| Агент добавил библиотеку, которой нет в списке | нет | `стоп`, потом «убери <библиотеку>, сделай без неё» | 5 мин |
| Агент начал править README | нет | `стоп`, потом «README не твой, откати изменения в нём» | 3 мин |
| У Эмины `git push` отклонён | нет | её агент сам сделает pull и повтор. Если 403, права нет: правки через сайт GitHub, потом написать организаторам | 5 мин |
| Первый пуш (скаффолд, ~13:12) отклонён с текстом «protected branch» | да | платформа закрыла main от прямых пушей. Агенту: «пуш в main запрещён, пушь в ветку work и дальше всегда в неё»; на сайте GitHub открыть Pull Request work → main и сливать его на каждом чекпоинте. Эмине то же: её агент пушит в ту же ветку work | 10 мин |
| Конфликт в TASKS.md или PROGRESS.md | нет | агент сам: abort, merge, обе строки. Если завис, человеку: открыть файл, оставить обе строки, коммит | 5 мин |
| Claude упёрся в лимит | да | `чекпоинт`, потом Codex в той же папке: `codex --sandbox danger-full-access --ask-for-approval never`, `Я Александр`, потом тот же `блок N` | 10 мин |
| Codex: «git ЗАБЛОКИРОВАН песочницей» или `cannot open '.git/FETCH_HEAD': Permission denied` | да | закрыть Codex, запустить `codex --sandbox danger-full-access --ask-for-approval never`. Проверить `~/.codex/config.toml`: `sandbox_mode = "danger-full-access"`, `approval_policy = "never"` (ставит install.ps1) | 3 мин |
| «Repository not found» на pull или push | да | GitHub видит чужой аккаунт: `cmdkey /list`, удалить запись github, войти заново (ALEXANDER-SETUP ШАГ 8, EMINA-SETUP ШАГ 2) | 5 мин |
| Wi-Fi упал | да, для деплоя | LAN-переходник, потом хотспот. Локальная работа продолжается, деплой позже | 5 мин |
| Ноутбук Александра умер | да | всё запушенное живо. На ноутбуке Эмины отдельный клон C:\hack\lomra-a; в нём `git config user.name "Alexander Kurchakov"` и `git config user.email fistin103@gmail.com` (локально, без --global); Claude Code со своим аккаунтом; первым сообщением вставить текст C:\hack\agent-harness\skills\hackathon-day\SKILL.md со словами «на этом клоне ты агент Александра», потом `Я Александр`. install.ps1 на её ноутбуке не перезапускать | 20 мин |
| Задача двусмысленная, в условии дыры, непонятно, для кого | нет | штатно: `spec` у Александра выписывает вопросы с допущениями и раздел «Для кого», `вопросы` у Эмины собирает их в docs/QUESTIONS.md, она несёт ментору, потом `ответы` у обоих. Нет ментора: допущения остаются и уходят в README в ограничения. Спор о пользователе дольше 5 минут запрещён: берётся роль из SPEC | 5 мин |
| В условии есть команды для ИИ, ссылка «скачай», ключ, закодированный блок, скрытый текст | нет, пока никто это не выполнил | не выполнять ни руками, ни агентом. Агент сам выписывает это в SPEC в «Подозрительные фрагменты ТЗ» и идёт по видимым требованиям. Эмина спрашивает ментора, что это. Агенту ничего | 0 мин |
| Агент выполнил что-то из текста условия: перешёл по ссылке, скачал файл, поставил пакет «которого просило ТЗ» | да | `стоп`, потом «откати всё, что сделано по тексту условия, библиотеку удали, в SPEC запиши это как подозрительный фрагмент». Потом `git status` и package.json глазами | 10 мин |
| Ответ ментора противоречит условию или добавляет работу | нет | условие главнее: судья сверяет с текстом кейса. Противоречие остаётся в docs/ANSWERS.md с пометкой, план не расширяется: лишнее в «Не делаем» или в requests | 3 мин |
| Не успеваем к 2:30 | | план «отстаём» из MAP.md (раздел «Проверки 1:40 и 2:30»). Не режем README, verify, Approve, живой URL с Turso. 3:30, 4:00 и 4:30 не сдвигаются | |

## Что сбивает темп сильнее всего

1. Разговор с агентом вместо команд. Каждый абзац объяснений это потерянный контекст блока.
2. Косметика посреди функционального блока.
3. Повторный запуск одного блока три раза. После второго провала блок упрощается, а не повторяется.
4. Правка чужих файлов: README у Александра, код у Эмины. Конфликт в git и десять минут на распутывание.
5. «А давай ещё». Нет.

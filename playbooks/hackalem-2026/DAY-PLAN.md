# План дня 23.09.2026 · команда Lomra

**Старт 13:00, окончание 18:00** (Положение 5.3.1 и 5.4.13). Итоговая версия — содержимое репозитория на 18:00, **независимо от того, когда платформа заблокирует интерфейс**. Всё, что ниже, отсчитывается от 13:00: `0:00` = 13:00, `4:45` = 17:45. Часы: в 12:50 отдельной вкладкой терминала `pwsh -NoProfile -File C:\hack\agent-harness\hack-clock.ps1 -Start 13:00`.
Роли: Александр (А) — код, деплой; Эмина (Э) — TASK.md, проверка SPEC, README, PROGRESS, чекпоинты, общение с оргами.
Два агента, один репозиторий. Агенту Александра команды `scaffold`, `spec`, `ответы`, `старт`, `го`, `блок N`, `чекпоинт`, `ревью`, `стоп` (скилл `hackathon-day`). Агенту Эмины на её ноутбуке команды `task`, `вопросы`, `ответы`, `progress`, `readme`, `testdata`, `audit`, `судья сборка`, `судья`, `submit` (скилл `captain`). Файлы разделены, доска общая: TASKS.md. Что делает каждая команда, в `templates/KICKOFF-PROMPT.md`.

## До старта (утро 23.09, всё до 12:30; потом 12:30–13:00)
Утро, строго по порядку (подробно с командами в `laptops/ALEXANDER-SETUP.txt`, раздел «УТРО 23.09»):
1. **Б2 Turso** (облачная SQLite; правила не требуют именно её, но только она работает с нашим кодом без переделок): две базы, `lomra-probe` для утреннего теста и `lomra` на день (до 13:00 не трогать). Делается после создания проектов Vercel. Путь A, без нового аккаунта: Vercel → Marketplace → **Turso Cloud**, бесплатный план, Connect Project только в Production и с **пустым Custom Prefix**, проверка `vercel env ls production` → ровно `TURSO_DATABASE_URL` и `TURSO_AUTH_TOKEN`. Путь B: app.turso.tech через GitHub, токены Read & Write, **Expiration: Never**, URL и токены в менеджер паролей. В обоих путях движок **libSQL** и регион **AWS US East (Virginia)**. Подробно в `skills/deploy-first`. `TURSO_*` живут только в env Vercel, **никогда в `.env.local`**: `pnpm verify` и `pnpm seed` сбросили бы боевую базу.
2. **Б3 OpenAI.** platform.openai.com: своя организация, проект **lomra-demo**, предоплата $15–20, **автопополнение ВЫКЛ**, ключ проекта в менеджер паролей. Этот ключ только для Vercel production.
3. **Б6 Vercel заранее.** Проект `lomra`, env production: `OPENAI_API_KEY` (lomra-demo), `TURSO_DATABASE_URL` и `TURSO_AUTH_TOKEN` (база `lomra`), `AGENT_MODEL`=gpt-6-sol (кандидат; замер в 1:50 и 2:50). Settings → Deployment Protection → Vercel Authentication **OFF**.
4. **Б4 Боевой прогон** на копии `C:\hack\rehearsal` (оригинал не трогать) с отдельным probe-проектом Vercel и базой `lomra-probe`: живой Run в браузере (стриминг), Approve, запись в Turso, в `vercel logs` нет ENOENT schema.sql, засечь длительность сборки и секунды Run. Ошибка про verification — новая организация OpenAI требует верификацию для стриминга: верифицировать организацию или взять другой ключ/модель. Потом probe-проект удалить: публичный URL с ключом не оставлять.
5. **Б7** параллельно с Б4: ноутбук Эмины по `laptops/EMINA-SETUP.txt`.
6. **Б8 Часы:** `pwsh -NoProfile -File C:\hack\agent-harness\hack-clock.ps1 -Start 13:00 -Once` печатает состояние и выходит; с `-ShiftMinutes` проверить надписи блоков (чекпоинт на :50 только до 4:40, после 17:40 крика нет).
7. **Б9 Заморозка комплекта до 12:30:** в `C:\hack\agent-harness` сначала `git status --short` пуст и `git rev-parse HEAD` = `git rev-parse origin/main`, иначе сначала commit+push; потом `git tag pre-start-2026-09-23`, `git push origin pre-start-2026-09-23`. После тега в комплект не коммитить; ошибку скилла днём чинить только локально в `~/.claude/skills`.

12:30–13:00:
- 12:30 перки в кабинете: Э забирает ключ API, А логинит Codex под перк-аккаунтом. Перк-ключ только в `C:\hack\lomra\.env.local` (`OPENAI_API_KEY=`, `AGENT_MODEL=gpt-6-luna`), в Vercel никогда.
- Если в перках нет API-ключа OpenAI, а только подписка ChatGPT: подписка приложению не подходит, модель внутри продукта работает только по ключу. Тогда ключ NVIDIA из перков в `.env.local` как `NVIDIA_API_KEY` плюс `AGENT_MODEL` с build.nvidia.com (модель с function calling); код один и тот же, lib/model.ts выбирает провайдера по env. Нет ни того, ни другого: свой ключ OpenAI в `.env.local`. Режим просмотра без ключа не проходит п. 5.6.6: в Vercel production ключ lomra-demo есть с утра (Б6) независимо от перков.
- А: `git clone` репозитория команды из кабинета, `pnpm -v`, `node -v`, `vercel whoami`, `git -C C:\hack\agent-harness status` чистый и тег на месте, Claude Code открыт в папке. Ничего не пушить.
- Э: открыт Telegram-канал хакатона, почта, страница «Треки» в кабинете.
- 5.1.7.1: оба на площадке с 12:30 до 18:00; выход с площадки только по одному и не больше 15 минут; Эмина ведёт в заметках телефона время каждой отлучки обоих, сумма на человека < 45 мин (запас к 60).
- 12:40 Тексты задач, если видны до 13:00, лежат **только в `C:\hack\pre\ALL-TASKS.md`, вне репозитория команды**; Александру Эмина шлёт их через Telegram. А прогоняет их через промпт из `PICK-TASK.md` во второй сессии Claude в `C:\hack\agent-harness`: только рейтинг, ни SPEC, ни файлов в lomra.
- Методика оценки и порядок отбора финалистов должны выйти до старта. Э читает первой и пересказывает А за 2 минуты: обязательные условия допуска и требуемые материалы.
- 12:50 А: часы отдельной вкладкой (`pwsh -NoProfile -File C:\hack\agent-harness\hack-clock.ps1 -Start 13:00`), `Я Александр` в сессии lomra. Э: `Я Эмина`. Оба агента отдельной строкой докладывают результат pull: «git: pull ок, запись в .git работает»; иное — чинить сейчас по `guide/04`.

## 0:00–0:22 · скаффолд, выбор кейса, SPEC
- Часы уже идут с 12:50 в отдельной вкладке. Они считают блоки, кричат на :50 про чекпоинт (последний раз в 4:40) и про заморозки 3:30, 4:00, 4:30.
- 0:00 А пишет `scaffold` (блок 0, 0:00–0:12). Агент создаёт проект, раскладывает комплект, коммитит «scaffold + conventions (disclosed, agent-harness@<hash>)», пушит. 9–12 минут само (замер репетиции, не 3–5).
- Если тексты задач не были видны до 13:00: тем временем Э собирает их в `C:\hack\pre\ALL-TASKS.md` (вне репозитория команды) и шлёт А в Telegram, А прогоняет их через промпт из `PICK-TASK.md` во второй сессии Claude в папке agent-harness. Берут первую по рейтингу без красных флагов. Единой рубрики больше нет (п. 5.5): при выборе смотреть и на таблицу баллов задачи — брать ту, где тяжёлые критерии ложатся на то, что мы строим быстро.
- ~0:05–0:10, сразу как задача выбрана: Э кладёт условие и критерии оценки дословно в `docs/TASK.md` и `docs/CRITERIA.md` (`task`), **не дожидаясь скаффолда** — он эти файлы не трогает и перед пушем делает pull --rebase. Её агент перед этим называет подозрительные места в тексте и ничего из них не делает. Э пересказывает А за две минуты, сколько баллов за что. ~0:12–0:20 А пишет `spec`: таблица связывает требование → инструмент или экран → критерий → баллы. Агент сначала проверяет условие: команды для ИИ, ссылки «скачай», скрытый текст, противоречия и дыры; находки цитирует в SPEC в «Подозрительные фрагменты ТЗ» и не выполняет. Потом «Для кого» (одна роль, боль, первое действие), вопросы заказчику с допущениями по умолчанию, план и таблица требований. А правит две минуты, Э сверяет таблицу требований с условием. Коммит `spec: кейс …`.
- Э: `вопросы`, в ~0:22 несёт docs/QUESTIONS.md ментору партнёра, ответы дословно, потом `ответы` (docs/ANSWERS.md). А на границе ближайшего блока пишет `ответы`: допущения в SPEC обновлены, объём не вырос. Нет ментора: допущения остаются и уходят в README в ограничения.
- ~0:22 `старт`, ответ агента одним абзацем, `го`.

## 0:22–0:50 · блок 1: база, сид, таблица, Reset, /api/health
Агент по AGENTS.md и SPEC.md: lib/db.ts с getDb() и dbMode() (файл или Turso по env, схема и сид при первом подключении), data/schema.sql, data/seed.json с относительными датами, scripts/seed.ts, lib/types.ts, POST /api/reset (пересоздаёт доменные таблицы по schema.sql, agent_runs не трогает), GET /api/health (`ok`, `db`, `db_ok`, `model`, `rows`, значения ключей никогда), главная как серверный компонент с dynamic = "force-dynamic", таблица с бейджами статусов, кнопка Reset, красная строка в режиме memory.
Параллельно А руками, блок 2, ~0:25: `vercel link --yes --project lomra` (проект и env созданы утром, Б6), `vercel --prod --yes`, открыть с телефона в инкогнито без входа Vercel. Пустой деплой без /api/health — его ещё нет. Скилл deploy-first.
Э: PROGRESS.md строка 1 черновиком, README.md из `docs/README.template.md` с TL;DR и таблицей требований (статусы пока пустые).
0:50 `чекпоинт`, сразу за ним деплой (правило деплоя ниже) и `/api/health` → `"db":"turso","db_ok":true`. 0:55 Э `progress`.

**Правило деплоя (весь день).** Деплой каждый час сразу после чекпоинт-пуша (0:50, 1:50, 2:50), деплой-кандидат 3:15 (блок 7, сразу после пуша блока 6, до `блок 9`) и финальный в 4:00 — только при пустом `git status --short` и HEAD = origin/main: vercel заливает рабочую папку, а не коммит. Начиная с деплоя, где есть блок 1, первая проверка — `/api/health`, ворота: `"db":"turso","db_ok":true`. Смоук после каждого деплоя начинается с Reset. В README идёт алиас production, который напечатал vercel; адрес не конструировать.

## 0:50–1:40 · блок 3: чтение агентом, маршрут, панель с трейсом
`/clear`, потом `блок 3`. Агент по скиллам ai-sdk-agent и agent-console: lib/agent.ts с createAgent() и инструментами чтения, маршрут app/api/chat/route.ts, панель с useChat и трейсом, цель по умолчанию из SPEC.md, уведомление без ключа.
Э: README раздел «Техническая реализация» по мере готовности; вопрос ментору партнёра: что заставит их запустить пилот. 1:30 Э `readme`.
1:40 проверка: Run показывает трейс? Нет — отстаём на час, резать по MAP. Это первый живой Run кода дня: один прогон на low, засечь секунды; сразу `/clear` и `блок 4` (замер уровней здесь сбил бы блок 4: агент правит lib/agent.ts, dev-сервер перезагружается).
1:50 `чекпоинт` + деплой: 3 Run на URL на low, трейс виден, засечь секунды каждого; если все быстрее 15 с — `AGENT_REASONING=medium` через `vercel env` и `vercel redeploy <URL> --target production`, ещё 1 Run. 1:55 Э `progress`.

## 1:40–2:30 · блок 4: запись с Approve, сценарий целиком
`/clear`, `блок 4`. Инструмент записи через toolApproval, карточка Approve/Reject, подсветка изменённой строки, итоговый ответ. А проверяет сценарий руками: Run, Approve, строка изменилась. 2:30 `ревью`: список нарушений по требованиям и правилам, группы 1 и 2 чинятся одним коммитом.
2:00 Э: `судья сборка` — её агент клонирует репозиторий в `C:\hack\judge-HHMM`, `pnpm install --frozen-lockfile`, `pnpm build`, результат в docs/JUDGE-CHECK.md. УПАЛ — строку ошибки она передаёт А дословно, это чинится первым.

## 2:30–3:15 · блоки 5 и 6: надёжность и доказательства
`блок 5` (2:30–2:50): zod на всех входах, ошибки в панели, лимиты, поведение без ключа. `блок 6` (2:50–3:15): тесты с мок-моделью, scripts/verify.ts, вывод verify и test в docs/verify.log и docs/test.log (в README их переносит `readme` Эмины в 3:15).
2:50 `чекпоинт` + деплой: Approve на URL, строка сохраняется после обновления. **Модель в Vercel выбирается здесь:** `gpt-6-sol`, если 3 из 3 Run на URL < 30 с с карточкой Approve и баланс lomra-demo ≥ $10; иначе `gpt-6-luna`; иначе `gpt-5.6-terra`. После 3:15 env не менять. Смена модели: `vercel env rm AGENT_MODEL production --yes`, `vercel env add AGENT_MODEL production`, потом `vercel redeploy <URL текущего production-деплоя> --target production` (рабочая папка не участвует). 2:55 Э `progress`. Codex параллельно может писать tests/tools.test.ts, если Claude занят.
Проверку чистого клона делает агент Эмины: `судья сборка` в 2:00 и `судья` в 4:00 (результат в docs/JUDGE-CHECK.md). Пояснений экспертам давать не дадут (п. 5.4.16, 5.6.5), поэтому УПАЛ в 2:00 чинится сразу, пока есть два часа. Промпт «Проверка как судья» в templates/KICKOFF-PROMPT.md — только запасной путь, если агент Эмины недоступен.

## 3:15–3:30 · блоки 7 и 9: деплой-кандидат и дизайн
- А руками, блок 7: `vercel --prod --yes` (правило деплоя выше), `/api/health`, смоук с телефона в инкогнито: Reset, Run, Approve, изменение видно. Запомнить URL последнего хорошего деплоя. URL в README только после смоука.
- Агент: `блок 9, 15 минут` — не больше; режется первым, если дизайна нет в критериях задачи. Потом скриншот главной в docs/screenshot.png.
- Э: 3:15 `readme`, README целиком по скиллу readme-rubric, раздел ограничений и раскрытия материалов. А проверяет каждую строку таблицы требований на правду.

## 3:30–4:00 · ревью и блок 10: security-pass
- **3:30 новое не начинать.** Дальше только ревью, security-pass и баги.
- `ревью`, потом `блок 10` (до 3:55): security-pass, результаты в README через requests, `pnpm smoke`, `pnpm verify`.
- Э: 3:35 живой URL на телефоне — Reset, Run, Approve, три обновления, тот же URL во втором браузере, скриншоты; 3:40 `audit`; 3:55 `progress`.
- 3:50 `чекпоинт`.

## 4:00–4:45 · финал
- **4:00 КОД ЗАМОРОЖЕН.** Финальный деплой только при пустом `git status --short` и HEAD = origin/main, `/api/health` → turso. Э: `судья` — её агент идёт по README буквально в чистом клоне (install, build, test, verify), проверяет 8 разделов 5.4.15 и реальный URL. Потом она руками `pnpm start` и живой URL в инкогнито.
- 4:00–4:25 только баги в форме «Баг: …»: фикс → пуш → деплой (то же правило) → `/api/health` и смоук; после 4:25 код не коммитить. 4:40 чекпоинт пушит только уже закоммиченное (обычно пусто: тогда строка 11 TASKS с хешем и URL). Фикс после 4:10 → Эмина повторяет `судья сборка`. Не прошла по README: А чинит этот шаг, а не добавляет.
- Модель не меняется: выбрана на деплое 2:50.
- ≤4:30 Э: `submit` и отправка анкеты после «СУДЬЯ: ПРОШЁЛ» и проверки URL.
- **4:30 ДЕПЛОЙ ЗАМОРОЖЕН**: дальше только `vercel rollback` на URL последнего хорошего деплоя.
- 4:35 Э: последний `progress`.
- 4:40 А: последний `чекпоинт`, `git log origin/main -1 --format=%h` совпадает с локальным, хеш Эмине.
- 4:45 Э: сверка хеша на GitHub с телефона. **После 17:45 не пушить никогда.**

## После 18:00 и до Demo Day 29.09
Эксперты проверяют 24–28.09, без нас и без наших аккаунтов (п. 5.6.6). Живой URL должен прожить неделю.
- В репозиторий ничего не пушить: итоговая версия зафиксирована на 18:00 (п. 5.4.13–5.4.14).
- Проект Vercel, базу Turso `lomra`, OpenAI-проект lomra-demo — не удалять, не переименовывать.
- Жёсткий потолок расходов — предоплата lomra-demo без автопополнения ($15–20; при gpt-6-sol прогон около $0.08). Суточный предохранитель приложения `AGENT_RUNS_PER_DAY` (300) — только от злоупотребления.
- 24–29.09 в 10:00 Э с телефона в инкогнито: `/api/health`, потом Reset → Run → Approve → Reset. А в тот же час смотрит баланс lomra-demo. Что-то умерло (ключ, баланс, база) — чинить только env в Vercel и Redeploy того же коммита (ротация: `vercel env rm`, `vercel env add`, Redeploy), код не деплоить. Перед первой такой правкой спросить организаторов. Подробно в `DEMO-DAY.md`.

## Если ломается
- Wi-Fi упал: LAN-переходник, потом хотспот; локальная разработка продолжается, деплой позже; чекпоинт можно показать локально.
- Перк-ключ не работает: свой ключ в .env.local. В Vercel production всегда ключ проекта lomra-demo, перк только в .env.local (deploy-first, Env matrix): перк могут отозвать после события, а эксперты проверяют живой URL 24–28.09.
- Живой Run на URL падает с ошибкой про verification: новая организация OpenAI требует верификацию для стриминга. Верифицировать организацию или сменить ключ/модель в env Vercel; код не трогать.
- API-ключа нет вообще (перк оказался подпиской, своего аккаунта нет): локально `NVIDIA_API_KEY` из перков и `AGENT_MODEL` с build.nvidia.com. Режим просмотра не проходит п. 5.6.6: продакшен-ключ (lomra-demo или свой NVIDIA) обязан быть в Vercel к деплою 1:50.
- Деплой падает: чинить до 20 минут, потому что живой URL это путь проверки для судьи без ключа. Не поднялся: это прямой риск недопуска по п. 5.6.6 — проверка ключевой функциональности должна идти без наших аккаунтов и ключей. Тогда в README честно: локальный запуск основной путь, и обязательно дать способ проверки без наших аккаунтов (`pnpm test`, `pnpm verify`, docs/verify.log); ключ в репозиторий не кладётся никогда.
- Turso не заводится (`/api/health` показывает не `"db":"turso","db_ok":true`): это ворота допуска, НЕ «одна строка в ограничениях». Без Turso на Vercel у каждого экземпляра функции своя база в памяти: запись идёт в одном, страница перерисовывается в другом, эксперт жмёт Approve и видит, что строка не изменилась — недопуск по п. 5.4.16. Страница в этом режиме показывает красную строку «Демо-база в памяти…». Чинить до 20 минут: новый токен (Expiration: Never) в app.turso.tech, `vercel env rm` и `vercel env add` для `TURSO_*`, `vercel --prod --yes`, `/api/health`. Не завелось: сразу сказать Эмине; в README основной путь проверки — локальный запуск, живой URL с красной строкой честно помечен, причина в ограничениях. Деплой в памяти за рабочий не выдавать.
- Reset или Approve на URL дают 500 после позднего блока: схема в Turso старая. Reset пересоздаёт доменные таблицы по schema.sql, поэтому смоук после деплоя всегда начинается с Reset.
- Агент не проходит сценарий за 6 шагов: упростить цель по умолчанию, уменьшить сид, не расширять инструменты.
- Codex или Claude Code «зависает» на задаче дольше 15 минут: новая сессия, задачу разбить пополам.
- Claude упёрся в лимит: доделать блок, `/clear`, дальше Codex CLI на том же репозитории: `codex --sandbox danger-full-access --ask-for-approval never` (без этого песочница Windows не даёт писать в .git).

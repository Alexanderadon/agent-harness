# План дня 23.09.2026 · команда Lomra

Старт соревновательной части ориентировочно 13:30, репозиторий платформа закроет автоматически через 5 часов. Время ниже
относительное от старта. Роли: Александр (А) — код, деплой; Эмина (Э) — SPEC, README, PROGRESS, чекпоинты, общение с оргами.

## До старта (12:30–13:30)
- 12:30 перки в кабинете: Э забирает ключ API, А логинит Codex под перк-аккаунтом. Свой ключ OpenAI уже в `.env.local` на первый час.
- А: `git clone` репозитория команды из кабинета, `pnpm -v`, `node -v`, `vercel whoami`, `turso auth status`, Claude Code открыт в папке.
- Э: открыт Telegram-канал хакатона, почта, страница «Треки» в кабинете, шаблоны SPEC/README/PROGRESS из agent-harness под рукой.
- Методика оценки и порядок отбора финалистов должны выйти до старта. Э читает первой и пересказывает А за 2 минуты: обязательные условия допуска и требуемые материалы.

## 0:00–0:15 · выбор кейса и SPEC
- Э читает все задачи своего трека и двух соседних, отсеивает по чеклисту: требования короткие, данные есть или легко сидируются, нет внешних систем с доступами, нет требования особого стека.
- А читает финалистов чеклиста, выбирает. Критерий один: какие обязательные требования закрываются полностью за 3 часа.
- Э заполняет SPEC.md по шаблону, А правит инструменты и сценарий. Коммит `spec: кейс …`.

## 0:15–0:35 · скаффолд и пустой деплой
Промпт А в Claude Code:
> Прочитай AGENTS.md и SPEC.md. Создай Next.js 16 приложение по конвенциям: pnpm, TypeScript strict, Tailwind v4, shadcn init, lucide, ai, @ai-sdk/openai, @ai-sdk/react, zod, @libsql/client, vitest, tsx. Добавь .nvmrc, .env.example из templates, lib/db.ts с выбором file:data/app.db или Turso, data/schema.sql и scripts/seed.ts по сущности из SPEC.md, POST /api/reset. Главная страница показывает таблицу из базы. Ничего про агента пока не делай. Закончи на pnpm typecheck и pnpm seed.

Параллельно А: `vercel link`, `vercel --prod --yes` на пустое приложение, Turso создать базу, env в Vercel. Скилл deploy-first.
Э: PROGRESS.md строка 1 черновиком, README.md из шаблона с TL;DR и таблицей требований (статусы пока пустые).

## 0:35–1:00 · данные и контракт
Промпт А:
> По SPEC.md сгенерируй data/seed.json: N записей, реалистичные казахстанские названия, k проблемных записей ровно таких, как описано в сценарии. lib/types.ts как единый контракт. Таблица на главной с бейджами статусов, кнопка Reset demo. Коммит.

Коммит-чекпоинт часа в 0:55, Э фиксирует хеш в PROGRESS.md.

## 1:00–2:30 · агент и трейс
Промпт А:
> Используй скилл ai-sdk-agent и agent-console. Сделай lib/agent.ts с ToolLoopAgent, инструментами из SPEC.md (чтения с execute, записи через toolApproval user-approval), маршрут app/api/agent/route.ts с createAgentUIStreamResponse и maxDuration 60, панель агента с useChat, трейсом по parts и карточками Approve. Цель по умолчанию из SPEC.md. Без ключа панель показывает уведомление. Проверь сценарий из SPEC.md руками и покажи мне, что изменилось в таблице.

Э: README разделы «Техническая реализация» и «Проверка основного сценария» по мере готовности, вопросы ментору партнёра: что заставит их запустить пилот.
Коммит-чекпоинт в 1:55.

## 2:30–3:15 · надёжность и доказательства
Промпт А:
> Используй скилл verify-script. zod на всех входах API с 400, обработка ошибок модели и инструментов в панели, лимит шагов и таймаут. tests/tools.test.ts и tests/agent.test.ts с MockLanguageModelV4. scripts/verify.ts по сценарию SPEC.md. pnpm smoke и pnpm verify должны быть зелёными. Вставь вывод verify и test в README раздел «Проверка».

Коммит-чекпоинт в 2:55.

## 3:15–3:45 · деплой со сценарием и README
- А: `vercel --prod --yes`, смоук с телефона: Reset, Run, Approve, изменение видно. URL в README только после смоука.
- Э: README целиком по скиллу readme-rubric, раздел ограничений и раскрытия материалов. А проверяет каждую строку таблицы требований на правду.

## 3:45–4:15 · дизайн и картинка
- А: скилл design-pass, 15 минут, потом скриншот и GIF 20 секунд в README. Э: PROGRESS.md, финальная вычитка README.
Коммит-чекпоинт в 3:55.

## 4:15–4:40 · финал
- А: скилл security-pass, 10 минут, результаты в README. Затем `pnpm smoke`, `pnpm verify`, чистый клон в соседнюю папку и запуск с нуля по README. Если что-то не так, чинить это, а не добавлять.
- Модель: весь день `AGENT_MODEL=gpt-5.6-luna`, перед финальным деплоем переключить на `gpt-5.6-terra` в Vercel env и прогнать сценарий ещё раз с телефона.
- Э: анкета подачи в кабинете заполнена черновиком к 4:30, отправлена в 4:40 после подтверждения А.
- 4:45 последний пуш. Дальше ничего не трогать.

## Если ломается
- Wi-Fi упал: LAN-переходник; локальная разработка продолжается, деплой позже; чекпоинт можно показать локально.
- Перк-ключ не работает: свой ключ в .env.local и в Vercel, перк подставить позже.
- Деплой падает: README честно указывает локальный запуск как основной путь; деплой не обязателен по рубрике.
- Агент не проходит сценарий за 6 шагов: упростить цель по умолчанию, уменьшить сид, не расширять инструменты.
- Codex или Claude Code «зависает» на задаче дольше 15 минут: новая сессия, задачу разбить пополам.

# План дня 23.09.2026 · команда Lomra

Старт соревновательной части ориентировочно 13:30, репозиторий платформа закроет автоматически через 5 часов. Время ниже
относительное от старта. Роли: Александр (А) — код, деплой; Эмина (Э) — TASK.md, проверка SPEC, README, PROGRESS, чекпоинты, общение с оргами.
Все сообщения агенту лежат готовыми в `templates/KICKOFF-PROMPT.md`, здесь только порядок.

## До старта (12:30–13:30)
- 12:30 перки в кабинете: Э забирает ключ API, А логинит Codex под перк-аккаунтом. Свой ключ OpenAI уже в `.env.local` на первый час.
- А: `git clone` репозитория команды из кабинета, `pnpm -v`, `node -v`, `vercel whoami`, Claude Code открыт в папке. Ничего не пушить.
- Э: открыт Telegram-канал хакатона, почта, страница «Треки» в кабинете.
- Методика оценки и порядок отбора финалистов должны выйти до старта. Э читает первой и пересказывает А за 2 минуты: обязательные условия допуска и требуемые материалы.

## 0:00–0:15 · скаффолд, выбор кейса, SPEC
- 0:00 А отправляет сообщение −1: скаффолд через скилл scaffold, перенос комплекта, коммит «scaffold + conventions (disclosed)», пуш. Работает 3–5 минут само.
- Тем временем Э читает все задачи своего трека и двух соседних, отсеивает по чеклисту: требования короткие, данные есть или легко сидируются, нет внешних систем с доступами, нет требования особого стека.
- А выбирает. Критерий один: какие обязательные требования закрываются полностью за 3 часа.
- Э кладёт условие дословно в `docs/TASK.md`. А отправляет сообщение 0: агент пишет `SPEC.md`. А правит две минуты, Э сверяет таблицу требований с условием. Коммит `spec: кейс …`.
- 0:15 сообщение 1, ответ агента одним абзацем, «го».

## 0:15–0:40 · блок 1: база, сид, таблица, Reset
Промпт блока (сообщение 2 с подстановкой):
> Блок 1 из TASKS.md: база, сид, таблица, Reset. Таймбокс 25 минут. Готово, когда pnpm dev показывает таблицу из сида и Reset возвращает сид. По SPEC.md: lib/db.ts с getDb() (file:data/app.db или Turso по env, схема и сид при первом подключении), data/schema.sql, data/seed.json с относительными датами, scripts/seed.ts, lib/types.ts как контракт, POST /api/reset, главная страница как серверный компонент с dynamic = "force-dynamic", таблица с бейджами статусов, кнопка Reset demo. Закончи на pnpm typecheck и pnpm seed. Го.

Параллельно А руками, блок 2: `vercel link`, `vercel --prod --yes` на текущее состояние, Turso URL и токен в env Vercel, Deployment Protection выключить, открыть с телефона в инкогнито. Скилл deploy-first.
Э: PROGRESS.md строка 1 черновиком, README.md из `docs/README.template.md` с TL;DR и таблицей требований (статусы пока пустые).
Коммит-чекпоинт часа в 0:55, Э фиксирует хеш в PROGRESS.md.

## 0:40–1:40 · блок 3: чтение агентом, маршрут, панель с трейсом
> Блок 3 из TASKS.md. Таймбокс 60 минут. Готово, когда Run показывает вызовы чтения в трейсе и итоговый текст. Используй скиллы ai-sdk-agent и agent-console: lib/agent.ts с createAgent() и инструментами чтения из SPEC.md, маршрут app/api/chat/route.ts с createAgentUIStreamResponse, maxDuration 60 и zod на теле запроса, панель агента с useChat и трейсом по parts, цель по умолчанию из SPEC.md, без ключа панель показывает уведомление. Го.

Э: README раздел «Техническая реализация» по мере готовности; вопрос ментору партнёра: что заставит их запустить пилот.
Коммит-чекпоинт в 1:55. Codex параллельно в 1:30: черновик README по сообщению 4.

## 1:40–2:30 · блок 4: запись с Approve, сценарий целиком
> Блок 4 из TASKS.md. Таймбокс 50 минут. Готово, когда сценарий из SPEC.md проходит: Run, Approve, строка меняется. Инструмент записи через toolApproval user-approval, карточка Approve/Reject в панели, подсветка изменённой строки, итоговый ответ. Проверь сценарий руками и покажи мне, что изменилось в таблице. Го.

## 2:30–3:15 · блоки 5 и 6: надёжность и доказательства
> Блок 5 из TASKS.md. Таймбокс 20 минут. Готово, когда мусорный ввод даёт 400, а без ключа панель показывает уведомление. zod на всех входах API, ошибки модели и инструментов в панели, лимит шагов и таймаут. Го.

> Блок 6 из TASKS.md. Таймбокс 25 минут. Готово, когда pnpm test зелёный без ключа и pnpm verify печатает PASS. Используй скилл verify-script: tests/tools.test.ts, tests/agent.test.ts с MockLanguageModelV4, scripts/verify.ts по сценарию SPEC.md. Вывод verify и test вставь в README раздел «Проверка основного сценария». Го.

Коммит-чекпоинт в 2:55. Codex параллельно в 2:50 может писать tests/tools.test.ts по сообщению 4, если Claude занят.

## 3:15–3:45 · блоки 7 и 8: деплой со сценарием и README
- А руками: `vercel --prod --yes`, смоук с телефона в инкогнито: Reset, Run, Approve, изменение видно. URL в README только после смоука.
- Э: README целиком по скиллу readme-rubric, раздел ограничений и раскрытия материалов. А проверяет каждую строку таблицы требований на правду.

## 3:45–4:15 · блок 9: дизайн и картинка
> Блок 9 из TASKS.md. Таймбокс 15 минут. Используй скилл design-pass. Потом скриншот главной в docs/screenshot.png. Го.

Э: PROGRESS.md, финальная вычитка README, GIF 20 секунд с экрана, если есть время.
Коммит-чекпоинт в 3:55.

## 4:15–4:40 · блок 10: финал
> Блок 10 из TASKS.md. Используй скилл security-pass, результаты списком в README раздел «Базовая надёжность и безопасность». Потом pnpm smoke и pnpm verify. Го.

- А руками: чистый клон в соседнюю папку, запуск с нуля по README. Если что-то не так, чинить это, а не добавлять.
- Модель: весь день `AGENT_MODEL=gpt-5.6-luna` локально, в Vercel env перед финальным деплоем `gpt-5.6-terra`, прогон с телефона ещё раз.
- Э: анкета подачи в кабинете заполнена черновиком к 4:30, отправлена в 4:40 после подтверждения А.
- 4:45 последний пуш. Дальше ничего не трогать.

## Если ломается
- Wi-Fi упал: LAN-переходник, потом хотспот; локальная разработка продолжается, деплой позже; чекпоинт можно показать локально.
- Перк-ключ не работает: свой ключ в .env.local и в Vercel, перк подставить позже.
- Деплой падает: чинить до 20 минут, потому что живой URL это путь проверки для судьи без ключа. Не поднялся: README честно называет локальный запуск основным путём, деплой по рубрике плюс, а не обязанность.
- Turso не заводится: деплой без него, состояние на публичном URL живёт в памяти и сбрасывается, одна строка в ограничениях.
- Агент не проходит сценарий за 6 шагов: упростить цель по умолчанию, уменьшить сид, не расширять инструменты.
- Codex или Claude Code «зависает» на задаче дольше 15 минут: новая сессия, задачу разбить пополам.
- Claude упёрся в лимит: доделать блок, `/clear`, дальше Codex CLI на том же репозитории.

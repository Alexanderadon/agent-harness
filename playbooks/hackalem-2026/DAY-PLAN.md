# План дня 23.09.2026 · команда Lomra

Старт соревновательной части ориентировочно 13:30, репозиторий платформа закроет автоматически через 5 часов. Время ниже
относительное от старта. Роли: Александр (А) — код, деплой; Эмина (Э) — TASK.md, проверка SPEC, README, PROGRESS, чекпоинты, общение с оргами.
Агенту пишутся команды в одно слово (скилл `hackathon-day`): `scaffold`, `spec`, `старт`, `го`, `блок N`, `чекпоинт`, `стоп`. Что делает каждая, в `templates/KICKOFF-PROMPT.md`.

## До старта (12:30–13:30)
- 12:30 перки в кабинете: Э забирает ключ API, А логинит Codex под перк-аккаунтом. Свой ключ OpenAI уже в `.env.local` на первый час.
- А: `git clone` репозитория команды из кабинета, `pnpm -v`, `node -v`, `vercel whoami`, Claude Code открыт в папке. Ничего не пушить.
- Э: открыт Telegram-канал хакатона, почта, страница «Треки» в кабинете.
- Методика оценки и порядок отбора финалистов должны выйти до старта. Э читает первой и пересказывает А за 2 минуты: обязательные условия допуска и требуемые материалы.

## 0:00–0:15 · скаффолд, выбор кейса, SPEC
- 0:00 А пишет `scaffold`. Агент создаёт проект, раскладывает комплект, коммитит «scaffold + conventions (disclosed)», пушит. 3–5 минут само.
- Тем временем Э собирает тексты задач в `docs/ALL-TASKS.md`, А прогоняет их через промпт из `PICK-TASK.md` во второй сессии Claude в папке agent-harness. Берут первую по рейтингу без красных флагов.
- Э кладёт условие дословно в `docs/TASK.md`. А пишет `spec`. Агент пишет `SPEC.md`, А правит две минуты, Э сверяет таблицу требований с условием. Коммит `spec: кейс …`.
- 0:15 `старт`, ответ агента одним абзацем, `го`.

## 0:15–0:40 · блок 1: база, сид, таблица, Reset
Агент по AGENTS.md и SPEC.md: lib/db.ts с getDb() (файл или Turso по env, схема и сид при первом подключении), data/schema.sql, data/seed.json с относительными датами, scripts/seed.ts, lib/types.ts, POST /api/reset, главная как серверный компонент с dynamic = "force-dynamic", таблица с бейджами статусов, кнопка Reset.
Параллельно А руками, блок 2: `vercel link`, `vercel --prod --yes`, Turso URL и токен в env Vercel, Deployment Protection выключить, открыть с телефона в инкогнито. Скилл deploy-first.
Э: PROGRESS.md строка 1 черновиком, README.md из `docs/README.template.md` с TL;DR и таблицей требований (статусы пока пустые).
0:55 `чекпоинт`, Э фиксирует хеш в PROGRESS.md.

## 0:40–1:40 · блок 3: чтение агентом, маршрут, панель с трейсом
`/clear`, потом `блок 3`. Агент по скиллам ai-sdk-agent и agent-console: lib/agent.ts с createAgent() и инструментами чтения, маршрут app/api/chat/route.ts, панель с useChat и трейсом, цель по умолчанию из SPEC.md, уведомление без ключа.
Э: README раздел «Техническая реализация» по мере готовности; вопрос ментору партнёра: что заставит их запустить пилот.
1:30 Codex параллельно: черновик README (промпт из KICKOFF). 1:55 `чекпоинт`.

## 1:40–2:30 · блок 4: запись с Approve, сценарий целиком
`/clear`, `блок 4`. Инструмент записи через toolApproval, карточка Approve/Reject, подсветка изменённой строки, итоговый ответ. А проверяет сценарий руками: Run, Approve, строка изменилась.

## 2:30–3:15 · блоки 5 и 6: надёжность и доказательства
`блок 5`: zod на всех входах, ошибки в панели, лимиты, поведение без ключа. `блок 6`: тесты с мок-моделью, scripts/verify.ts, вывод verify и test в README.
2:55 `чекпоинт`. 2:50 Codex параллельно может писать tests/tools.test.ts, если Claude занят.

## 3:15–3:45 · блоки 7 и 8: деплой со сценарием и README
- А руками: `vercel --prod --yes`, смоук с телефона в инкогнито: Reset, Run, Approve, изменение видно. URL в README только после смоука.
- Э: README целиком по скиллу readme-rubric, раздел ограничений и раскрытия материалов. А проверяет каждую строку таблицы требований на правду.

## 3:45–4:15 · блок 9: дизайн и картинка
`блок 9, 15 минут`. Потом скриншот главной в docs/screenshot.png. Э: PROGRESS.md, финальная вычитка README, GIF 20 секунд, если есть время. 3:55 `чекпоинт`.

## 4:15–4:40 · блок 10: финал
`блок 10`: security-pass, результаты в README, `pnpm smoke`, `pnpm verify`.
- А руками: чистый клон в соседнюю папку, запуск с нуля по README. Если что-то не так, чинить это, а не добавлять.
- Модель: весь день `AGENT_MODEL=gpt-5.6-luna` локально, в Vercel env перед финальным деплоем `gpt-5.6-terra`, прогон с телефона ещё раз.
- Э: анкета подачи в кабинете заполнена черновиком к 4:30, отправлена в 4:40 после подтверждения А.
- 4:45 последний пуш. Дальше ничего не трогать.

## Если ломается
- Wi-Fi упал: LAN-переходник, потом хотспот; локальная разработка продолжается, деплой позже; чекпоинт можно показать локально.
- Перк-ключ не работает: свой ключ в .env.local и в Vercel, перк подставить позже.
- Деплой падает: чинить до 20 минут, потому что живой URL это путь проверки для судьи без ключа. Не поднялся: README честно называет локальный запуск основным путём, деплой по рубрике плюс, а не обязанность.
- Turso не заводится: деплой без него, состояние на публичном URL живёт в памяти и сбрасывается, одна строка в ограничениях.
- Агент не проходит сценарий за 6 шагов: упростить цель по умолчанию, уменьшить сид, не расширять инструменты.
- Codex или Claude Code «зависает» на задаче дольше 15 минут: новая сессия, задачу разбить пополам.
- Claude упёрся в лимит: доделать блок, `/clear`, дальше Codex CLI на том же репозитории.

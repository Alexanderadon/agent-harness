# agent-harness

Универсальное окружение для разработки агентных веб-продуктов с AI-инструментами (Claude Code, Codex) в 2026 году:
конвенции для агента-разработчика, скиллы, шаблоны документов, дизайн-токены, конфиги. Кода продукта здесь нет,
всё одинаково применимо к любому проекту: хакатон, пилот для заказчика, личный продукт.

Стек по умолчанию: Next.js 16, TypeScript strict, Tailwind v4, shadcn/ui, Vercel AI SDK v7, zod, libSQL (файл локально, Turso в облаке), vitest, pnpm, Vercel.

## Что внутри

- `AGENTS.md`, `CLAUDE.md` — конвенции для Claude Code и Codex: стек, структура, контракт, агент с подтверждением записи, стиль, надёжность, безопасность, что считается готовым.
- `skills/` — скиллы для агента-разработчика:
  - `scaffold` — минута ноль: Next.js 16 + shadcn + AI SDK без единого вопроса внутри непустого репозитория, плюс перенос комплекта из agent-harness.
  - `hackathon-day` — протокол дня для агента Александра: команды `spec`, `старт`, `го`, `блок N`, `чекпоинт`, `стоп`.
  - `captain` — протокол дня для агента капитана: `task`, `progress`, `readme`, `testdata`, `audit`, `submit`; только документы и данные, синхронизация через git.
  - `ai-sdk-agent` — агент на AI SDK v7: ToolLoopAgent, инструменты с zod, подтверждение записи, стрим в интерфейс, тесты с мок-моделью, модели и цены.
  - `agent-console` — экран «таблица слева, агент с трейсом и Approve справа, Reset».
  - `readme-rubric` — README, по которому проект можно оценить и запустить без автора.
  - `verify-script` — `pnpm verify`: доказательство, что основной сценарий работает.
  - `deploy-first` — Vercel через CLI, Turso, смоук с телефона.
  - `design-pass` — 15-минутный визуальный проход по фиксированной формуле.
  - `security-pass` — 10-минутная проверка безопасности и надёжности перед сдачей.
- `templates/` — README, SPEC, TASK (условие дословно), TASKS (чеклист блоков), PROGRESS, KICKOFF-PROMPT (что писать агенту), `.env.example`, дизайн-токены для `globals.css`.
- `codex/config.toml` — конфиг Codex.
- `install.ps1` — раскладывает скиллы в `~/.claude/skills` и `~/.codex/skills`, конфиг в `~/.codex`.
- `prewarm.ps1` — прогрев ноутбука: кладёт в кэш pnpm все пакеты дня и проверяет скаффолд и сборку.
- `hack-clock.ps1` — часы хакатона: от времени старта считает блоки, на :50 кричит «чекпоинт», за 10 минут до подачи кричит «подача».
- `playbooks/hackalem-2026/` — плейбук под конкретное событие, HackAlem AI 23.09.2026: карта действий (MAP), EMINA (день капитана на одном экране), PITFALLS (чужие ошибки и чем они закрыты), CONTEXT (память для агента на ноутбуке), PICK-TASK (выбор задачи в 13:30), план дня, форма продукта под рубрику (WINNING-SHAPE), репетиция, чеклист ноутбука, бюджеты мощности, DEMO-DAY (открывать после 23-го). Следующее событие получает свою папку рядом.

## Установка

```powershell
git clone https://github.com/Alexanderadon/agent-harness
cd agent-harness
.\install.ps1 -Role alexander    # ноутбук разработчика
.\install.ps1 -Role emina        # ноутбук капитана
```

Параметр Role записывает роль ноутбука в `~/.claude/CLAUDE.md` и `~/.codex/AGENTS.md`. В день первое сообщение агенту это имя: `Я Александр` или `Я Эмина`, агент подтверждает роль, проверяет git-имя и показывает свои команды.

В репозиторий проекта копируются `AGENTS.md`, `CLAUDE.md` и нужные шаблоны из `templates/` (см. `templates/KICKOFF-PROMPT.md`, шаг −1).

## Раскрытие на соревнованиях

Если правила требуют раскрыть заранее созданные материалы, строка для README проекта:

> До старта подготовлены и опубликованы общие материалы команды (репозиторий agent-harness, коммиты с датой до старта):
> конвенции AGENTS.md, шаблоны README/SPEC/PROGRESS, дизайн-токены, чеклисты. Весь код проекта написан
> во время соревновательной части в репозитории, созданном платформой. Использованы Claude Code и Codex.
